# Critique 178 — Review of innovation cycle cap implementation

**Reviewer**: elf (adversarial review)
**Scope**: 2 elf-authored commits — `be4d3e3` (innovation cycle cap) + `75d5090` (typecheck fix) + 2 housekeeping commits

## Review

### Compliance with work item

1. Add innovation cycle counting ✓ — `computeProcessPatterns` now returns `innovationCycleCount`
2. Expose in world state ✓ — via `Assessment.processPatterns.innovationCycleCount`
3. Modify tree condition ✓ — `innovationTier()` checks cycle count against config cap
4. Add config option ✓ — `maxInnovationCycles` with default 3
5. Tests ✓ — 4 new tests (2 in tick, 2 in shift-log-parser)

### Implementation quality

**`src/log/shift-log-parser.ts`**: Added `innovationCycleCount` — simple `actions.filter(a => a === "innovate").length`. Clean. ✓

**`src/types.ts`**: Extended `processPatterns` and `Config` types. ✓

**`src/config/load-config.ts`**: Added `max-innovation-cycles` to known keys, defaults, and parsing. Follows existing `intOrDefault` pattern. ✓

**`src/tree/default-tree.ts`**: The `innovationTier` function now checks `cycleCount < maxCycles`. Uses nullish coalescing defaults (`?? 3` and `?? 0`). Clean and safe. ✓

**`src/__tests__/tick.test.ts`**: Two new tests — one verifies routing to explore when cap reached, one verifies routing to innovate when below cap. Both provide full Config objects. ✓

### Typecheck fix

The second commit (`75d5090`) fixes a missing `maxInnovationCycles` field in `src/__tests__/setup.test.ts`. This is correct — the new required Config field needed to be added to all test Config objects. ✓

### Potential concerns

1. **The initial commit broke typecheck**: The first commit didn't update all Config objects in tests. The second commit fixed it. This is a minor process issue — ideally the elf would have run `npx tsc --noEmit` before committing. But the fix was immediate and correct.

2. **Counting only "innovate" actions, not "evaluate-insight"**: The cycle count only counts `innovate` actions, not the full innovate → evaluate → review cycle. This means the cap triggers after 3 innovate ticks, even if none were evaluated yet. This seems intentional per the work item ("count how many innovate actions have occurred").

## Issues Found

None blocking.

## Verdict

**Clean.** Well-implemented feature with one minor process issue (typecheck broken then fixed in the next commit).

## Status

Resolved.
