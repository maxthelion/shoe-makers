---
type: critique
date: 2026-03-22
commits: 9ba59ee..bbf5acd
status: mostly-clean
---

# Critique: Review of 9ba59ee..bbf5acd (24 commits)

## Commits reviewed

Substantive code changes:
- `4db6c08` — Enforce TDD: executor cannot write test files
- `47b7240` — Fix evidence patterns for plan lifecycle invariants
- `a691e64` — Fix TDD enforcement evidence patterns to match implementation
- `989c182` — Deduplicate runTests: export from assess.ts, import in verify.ts
- `e8b6066` — Refactor: extract accumulateClaims helper in invariants.ts
- `c1e8e2c` — Document comment-stripping behavior in claim-evidence.yaml
- `066b390` — Add config edge case tests: malformed values, empty skills, no-colon lines

Remaining commits are explore/prioritise bookkeeping, session logs, and findings.

## Assessment by area

### TDD enforcement (4db6c08, a691e64) — Good

The executor is now forbidden from writing to `src/__tests__/`. This is a clean, correct change:
- `cannotWrite` takes priority over `canWrite` in the permission check logic
- Tests cover both the positive (executor blocked) and negative (fix-tests allowed) cases
- The tdd-enforcement test was correctly updated to remove the now-false assertion

**Minor note**: The evidence patterns for `verification.tdd-enforcement` were loosened — `[cannotWrite, __tests__]` just checks that these strings exist somewhere in source. A more precise pattern would anchor to the specific role map entry. However, given the comment-stripping context documented in c1e8e2c, this is acceptable.

### Evidence pattern fixes (47b7240) — Questionable

Changed three plan lifecycle evidence patterns from exact strings like `"status: done"` to regex-style `"blocked|done"` with `frontmatter`. This is looser matching — it will match any source file containing the word "blocked" or "done" near "frontmatter". The old patterns were more specific. This feels like evidence was adjusted to pass rather than code being adjusted to satisfy evidence. Worth watching.

### runTests deduplication (989c182) — Clean

Exported `runTests` from `assess.ts` and imported it in `verify.ts`, removing the duplicate. Straightforward, correct refactoring.

### accumulateClaims extraction (e8b6066) — Clean

Extracted duplicated claim-classification loop into a helper function. The refactoring is behaviour-preserving — same logic, just DRYed up. The mutable counters object passed by reference is a reasonable pattern here.

### Config edge case tests (066b390) — Minor concern

The `NaN` test for non-numeric tick-interval documents current behavior but doesn't actually assert correctness. `NaN` as a tick interval would likely cause bugs downstream. The test just says "yes it's NaN" rather than testing that the system handles this gracefully (e.g., falls back to default). This is testing-what-happens rather than testing-what-should-happen.

### Comment-stripping documentation (c1e8e2c) — Clean

Added a helpful header comment to claim-evidence.yaml explaining that patterns match against comment-stripped source. Good documentation.

## Summary

The batch is largely solid. The main concern is the evidence pattern loosening in 47b7240 — changing exact-match patterns to broader regex patterns risks false-positive evidence matches. The config NaN test documents behavior without asserting correctness. Everything else is clean refactoring and well-tested enforcement.

## Resolution

The NaN config issue was fixed (loadConfig now falls back to default for non-numeric values). On further investigation, the evidence pattern change for plan lifecycle is actually correct — `"blocked|done"` matches the regex literal in the source code at assess.ts:54, which is specific to the implementation.

## Status

Resolved.
