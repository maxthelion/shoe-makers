---
type: critique
status: resolved
date: 2026-03-25
reviewed-range: 69b0178..c76c23f
---

# Critique 211 — Run-skill missing cases fix + shift test coverage

## Summary

The commit adds missing `innovate` and `evaluate-insight` cases to `run-skill.ts` and adds test coverage for the shift orchestrator (max-ticks outcome, onTick callback).

## Source changes (run-skill.ts)

**Good**: The two missing cases follow the exact same pattern as existing cases. The messages are descriptive and consistent. The bug was real — these valid ActionType values were falling through to "Unknown action".

## Test changes

**run-skill.test.ts**: Three new tests (innovate, evaluate-insight, dead-code) follow the existing test pattern. Tests verify both the action name and a semantically meaningful word ("insight", "dead-code") appear in the output. Adequate.

**shift.test.ts**:

- **max-ticks test**: Good — verifies that 3 consecutive explore ticks hit the limit and all steps are explore. Properly uses maxTicks: 3.
- **onTick callback test**: Good — verifies callback receives both steps in an explore→prioritise sequence.
- **"sleep" test (mislabelled)**: The test is titled "returns sleep when tree produces no action" but it does NOT actually test the sleep path. The default tree always returns "explore", so this test exercises the max-ticks path with maxTicks: 1. The extensive comments acknowledge this limitation. The test still provides value (verifying onTick works), but the title is misleading. **Advisory** — not blocking since the sleep path is unreachable with the current default tree anyway.

## Compliance check

- **Permitted actions**: Modified source files and test files as instructed by the work item. No out-of-scope changes.
- **No tests deleted or skipped**: Correct.
- **Tests pass**: 667 tests, 0 failures.
- **No side effects**: Pure function agent pattern followed.

## Verdict

**Clean with advisory note** — the sleep test title should be renamed to reflect what it actually tests (onTick with single-tick limit), but this is cosmetic. The core fix (missing run-skill cases) is correct and well-tested.

## Status

Resolved.
