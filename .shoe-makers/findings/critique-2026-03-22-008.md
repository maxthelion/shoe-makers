# Critique: Commits 452fa45..e872464

## Summary

Review of 16 commits (8 substantive, 8 shift log updates) covering: skill maps-to collision fix, wiki doc-sync for 4 pages, and a spec-code audit finding.

## Verdict: Approved (advisory notes below)

The work is correct, well-tested, and improves spec-code alignment. No blocking issues.

## What was done well

1. **Collision fix is a real bug fix.** Three skills sharing maps-to values with other skills (`bug-fix → fix`, `dead-code → health`, `octoclean-fix → health`) would cause non-deterministic `findSkillForType` results. Giving each a unique maps-to value is the right fix.

2. **Collision detection test prevents regression.** The new test iterates all skills and checks for duplicate maps-to values — simple, effective, and catches future collisions.

3. **Wiki updates are accurate.** Verified the verification.md permissions table against `src/verify/permissions.ts` — all 11 action types match (role names, canWrite, cannotWrite patterns). The observability.md updates correctly describe the current shift log format and ShiftSummary. The skills.md restructuring correctly moves octoclean-fix, bug-fix, and dead-code from "planned" to documented.

4. **Scheduled-tasks.md additions are accurate.** Working hours and branch management sections match `src/schedule.ts` and `src/setup.ts`.

## Advisory notes

### Mixed action types in one session
The elf appears to have done both a bug fix (skill collision — modifying `.shoe-makers/skills/` and `src/__tests__/`) and doc-sync (updating `wiki/pages/`) in the same session. Ideally, each setup cycle produces one scoped action. The fix was legitimate and valuable, but future elves should re-run `bun run setup` between different types of work to stay within role boundaries.

### Finding references dead code that may not be dead
The spec-code-audit finding lists `src/skills/verify.ts`, `src/skills/prioritise.ts`, and `src/skills/work.ts` as dead code (no production call sites). These are core skill modules that are called by `run-skill.ts` indirectly (the dispatch currently returns messages rather than executing). They're part of the architecture and will be needed when the agent execution pipeline is wired up. Labelling them "dead" could mislead a future elf into removing them.

### The `readLastAction` export note is valid
`readLastAction` in `src/state/last-action.ts` is exported but never called in production. This is genuinely unused — the reviewer prompt tells the elf to read `last-action.md` directly.

## Status

Resolved.
