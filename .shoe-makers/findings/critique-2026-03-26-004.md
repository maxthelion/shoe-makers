# Critique: 8deded6..HEAD

**Reviewer**: adversarial elf
**Date**: 2026-03-26
**Commits reviewed**: 2 commits (prioritise + creative corpus implementation)

## Summary

Prioritise commit picked local creative corpus implementation from candidates. Execute commit implemented `readLocalCorpus`, `pickFromCorpus`, `markArticleUsed` functions in `src/creative/wikipedia.ts`, updated `fetchRandomArticle` to use local corpus as second-tier fallback (Wikipedia → corpus → hardcoded), and updated `fetchArticleForAction` to mark corpus articles as used after innovate ticks.

## Criteria

### 1. Did the elf stay within its permitted files?
**Compliant.** Prioritise commit: `.shoe-makers/archive/state/`, `.shoe-makers/log/` — permitted for prioritiser. Execute commit: `src/creative/wikipedia.ts`, `src/__tests__/creative-corpus.test.ts`, `src/setup.ts` — executor role permits `src/`.

### 2. Does the code correctly implement what was asked?
**Compliant.** The work item specified: (1) `readLocalCorpus` reads corpus dir, parses frontmatter, filters used articles — done. (2) `pickFromCorpus` picks random unused article — done. (3) `markArticleUsed` adds `used: true` to frontmatter — done, handles both existing and missing frontmatter cases. (4) `fetchRandomArticle` updated to try corpus after Wikipedia fails — done, returns `corpusFilepath` for tracking. (5) `fetchArticleForAction` marks articles used — done. (6) `setup.ts` passes `repoRoot` — done.

### 3. Do the tests actually verify the behaviour, or are they trivial?
**Compliant.** 13 new tests across `creative-corpus.test.ts`. Tests verify: reading articles from dir, filtering used articles, empty directory handling, filename-as-title fallback, empty body skipping, summary truncation, pickFromCorpus null cases, markArticleUsed preserving frontmatter fields, adding frontmatter when none exists, and the full lifecycle (read → mark → re-read filters out).

### 4. Were any invariants or evidence patterns modified to game the system?
**Compliant.** No changes to `.shoe-makers/invariants.md` or claim-evidence files.

### 5. Does the change match the wiki spec?
**Compliant.** Implementation matches invariants.md claims about corpus management. The `parseFrontmatter` function is simple but sufficient for the corpus file format.

## Verdict

Overall: Compliant. Clean implementation of the local creative corpus per spec.

## Status

Resolved.
