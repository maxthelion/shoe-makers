# Critique: Commits ba992ef..2feec08

**Reviewer**: elf (adversarial review)
**Date**: 2026-03-22
**Scope**: 42 commits — fragile filter fix, KNOWN_KEYS tests, config/health logging, init.ts template extraction, invariants exclusion update

## Summary

Multiple elves worked through an explore→prioritise→implement cycle across several areas. The substantive code changes are:

1. **`buildSuggestions` includeFindings option** (2901912): Replaced the fragile `.filter(s => !s.includes("finding(s)"))` in setup.ts with an `{ includeFindings: boolean }` options parameter. This directly addresses the advisory from critique-064.
2. **KNOWN_KEYS sync tests** (471f504): Added two config tests — one verifying known keys produce no warnings, one verifying unknown keys do warn.
3. **Config logging in setup** (afc1894): Logs `tickInterval` and `maxTicksPerShift` during setup.
4. **Health score + worst files logging** (8a32428, a3067e3): Shows health score and top-3 worst files in setup output.
5. **init.ts template extraction** (76efadd): Moved ~260 lines of template strings from `init.ts` to `init-templates.ts`, reducing init.ts from 419 to 162 lines.
6. **Invariants exclusion** (f5e43d4): Added `init-templates.ts` to EXCLUDED_TOP_LEVEL so it's not flagged as unspecified.

## Detailed Review

### 1. buildSuggestions includeFindings — Clean

The fix is correct and directly addresses the previous critique's advisory. The `options?.includeFindings !== false` pattern means findings are included by default (backwards compatible) and only excluded when explicitly opted out. Two new tests verify both paths. Good.

### 2. KNOWN_KEYS sync tests — Clean, minor observation

Tests properly spy on `console.warn` and filter for "Unknown config key" messages. They restore the spy after each test. The "no warning for known keys" test covers all 6 known keys, which serves as a sync check — if a new config key is added but not to KNOWN_KEYS, this test won't catch it (it only tests that existing known keys don't warn). Still, the "warns on unknown key" test is the important one and it works correctly.

### 3. Config and health logging — Clean

Simple `console.log` additions in `logAssessment()`. The worst-files display slices to top 3 and only shows when health < 100. No logic errors.

### 4. init.ts template extraction — Clean, mechanical

Pure move of template string constants from `init.ts` to `init-templates.ts`. All 9 constants are exported and imported correctly. The templates themselves are unchanged (verified via diff — only the `const` → `export const` change). No behaviour change.

### 5. Invariants exclusion — Correct

Adding `init-templates.ts` to EXCLUDED_TOP_LEVEL is necessary after the extraction. Without it, the invariants checker would flag the new file as unspecified.

### 6. Session log entries — Administrative

The many "Update session log with setup entries" commits are purely administrative logging. No code changes.

## Issues Found

**None blocking.** The work across these commits is clean. The previous critique's advisory (fragile string filter) was properly addressed. Tests pass (264 total, 0 failures). The template extraction is a safe mechanical refactoring.

### Minor observation (non-blocking)

The explore→prioritise→implement cycle generates a lot of ceremony commits (candidates.md, work-item.md, session log updates) relative to the actual work. For example, the config logging change (one line of code) went through 4 commits. This isn't a code quality issue, but it inflates the commit history. Not actionable by elves — it's a process design observation.

## Status

Resolved.
