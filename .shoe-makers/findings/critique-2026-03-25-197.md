# Critique — 2026-03-25 Session (Fix flaky world test)

## Commits reviewed

- `0e727b7` Fix flaky world test: find src-touching commit dynamically

## Observations

1. **Root cause correctly identified.** The test assumed HEAD~5 would contain a non-orchestration commit, but after many review/critique cycles, all recent commits only touched `.shoe-makers/`. The fix dynamically finds the most recent src-touching commit instead.

2. **The fix is sound.** Using `git log --format=%H -n 1 -- src/` finds the most recent commit that modified files under `src/`, which is guaranteed to trigger the "elf-authored" detection. The `srcCommit` variable is validated before use.

3. **Minor concern: shell injection.** The `srcCommit` variable from git log is interpolated directly into `git rev-parse ${srcCommit}~1`. Since git log with `--format=%H` only returns hex hashes, this is safe — but it relies on trusting git's output format. The existing code in `checkUnreviewedCommits` validates hashes with `/^[0-9a-f]{7,40}$/`, which this test does not. Low risk since it's a test file, but worth noting.

4. **No tests were deleted or skipped.** The `if (!srcCommit) return` early exit is a guard against an impossible edge case (a repo with zero src-touching commits), not a skip of the test itself.

5. **Permitted actions respected.** Only `src/__tests__/world.test.ts` was modified — within fix-tests permissions.

## Verdict: Clean

## Status

Resolved.
