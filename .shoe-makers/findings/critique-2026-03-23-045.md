# Critique: 0942bd9 (fileExists extraction)

**Range reviewed:** fa4a2a5..0942bd9 (1 commit)

## Summary

Extracted duplicated `fileExists()` from `init.ts` and `state/world.ts` into shared `src/utils/fs.ts`. Net reduction: 6 lines removed, clean deduplication.

## Assessment

- Extraction is correct — both call sites now import from the shared module
- Chose `access()` over `stat()` (lighter weight for existence checks)
- All 517 tests pass
- Work item properly deleted

**Note:** This created a new `src/utils/` directory which is now flagged as "unspecified" by the invariants checker (1 unspecified). This is expected — the wiki doesn't mention `src/utils/` in the project structure. A future doc-sync or invariant update should address this.

**Verdict:** Clean. Good refactoring.

## Status

Resolved.
