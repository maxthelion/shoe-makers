# Critique: 36dae2e..HEAD

**Reviewer**: adversarial elf
**Date**: 2026-03-26
**Commits reviewed**: 1 commit (structured critique skill implementation)

## Summary

Implements the structured critique skill per `wiki/pages/structured-skills.md`. Setup now gathers diff, commit log, last action, and auto-numbered filename before the critique elf starts. The prompt includes pre-filled sections with `[YOUR JUDGEMENT HERE]` placeholders. Legacy backward compatibility preserved.

## Criteria

### 1. Did the elf stay within its permitted files?
**Compliant.** The executor role permits `src/`, `.shoe-makers/state/`, `.shoe-makers/claim-evidence.yaml`. Files modified: `src/prompts/critique.ts`, `src/prompts/index.ts`, `src/prompts/reactive.ts`, `src/setup.ts`, `src/setup/format-action.ts`, new `src/setup/critique-context.ts`, new `src/__tests__/critique-prompt.test.ts`, new `src/__tests__/critique-context.test.ts`. All within `src/`.

### 2. Does the code correctly implement what was asked?
**Compliant.** The work item specified: (1) gather context in setup.ts — done via `gatherCritiqueContext`, (2) update `buildCritiquePrompt` to accept structured context — done with `CritiqueContext` interface, (3) thread through formatAction and generatePrompt — done, (4) preserve legacy fallback — done with `buildLegacyCritiquePrompt`. The auto-numbering checks both `findings/` and `findings/archive/`.

### 3. Do the tests actually verify the behaviour, or are they trivial?
**Compliant.** 20 new tests across 2 files. `critique-prompt.test.ts` verifies: context fields appear in output, structured sections present, placeholders present, permission violations included/omitted, status format guidance, legacy backward compat (no args, array args, empty array). `critique-context.test.ts` verifies: auto-numbering starts at 001, increments past existing, checks archive, takes max across both dirs, ignores non-matching files.

### 4. Were any invariants or evidence patterns modified to game the system?
**Compliant.** `.shoe-makers/invariants.md` untouched. No claim-evidence files modified.

### 5. Does the change match the wiki spec?
**Compliant.** The implementation matches `wiki/pages/structured-skills.md` sections on `write-critique`: setup provides diff, last-action summary, changed file list, commit range. Template defines filename (auto-numbered), sections, status format. The output format matches the spec example closely.

## Verdict

Overall: Compliant. Clean implementation matching the structured skills spec.

## Status

Resolved.
