# Critique 170 — Review of innovate insight (provenance chains)

**Reviewer**: elf (adversarial review)
**Scope**: 1 elf-authored commit — `cf4de1a` (innovate insight) + 1 housekeeping commit

## Review

### Compliance with innovate rules

1. Write an insight file ✓ (`2026-03-23-002.md`)
2. All four required sections (Lens, Connection, Proposal, Why) ✓
3. No off-limits files modified ✓

### Content quality

- **Lens**: Chain of custody — a real forensic/legal concept, clearly explained. ✓
- **Connection**: Correctly identifies that the system's state files are consumed (deleted) and decision context is lost. The observation that the shift log captures actions but not the decision graph is accurate. ✓
- **Proposal**: Adds YAML frontmatter provenance to state files and commit message conventions. Concrete — specifies the format and gives examples. ✓
- **Why**: Argues that provenance enables meta-analysis of decision quality. Reasonable claim. ✓

### Potential concerns

- The proposal adds YAML frontmatter to `candidates.md` and `work-item.md`. These files are currently freeform markdown with minimal structure. Adding frontmatter would require the consumer (prioritise/execute) to parse and understand it. This adds complexity to the three-phase prompts.
- The commit message convention (`Implements: work-item from candidates-...`) assumes the setup script constructs commit messages — but currently the elf commits its own work. The convention would need to be in the elf's prompt, not enforced programmatically.
- "grep-friendly provenance trail in git history" is clever but fragile — it depends on elves consistently following the convention.

## Issues Found

None.

## Verdict

**Clean.** Well-formed insight with all required sections. The idea is interesting but would need significant refinement at the evaluate-insight stage.

## Status

Resolved.
