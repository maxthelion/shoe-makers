# Critique — 2026-03-25 Session (Review-loop circuit breaker)

## Commits reviewed

- `f9829ef` Add review-loop circuit breaker to behaviour tree

## Observations

1. **Clean, minimal change.** Added one condition function (`inReviewLoop`) and one tree entry (`review-loop-breaker`) positioned between `tests-failing` and `unresolved-critiques`. The JSDoc tree diagram was updated to match.

2. **Correct priority positioning.** The circuit breaker fires after tests-failing (so broken tests still get fixed) but before unresolved-critiques (so it can break the loop). When the loop count is ≥3, it routes to explore, which resets the cycle.

3. **Good test coverage.** Three behavioral tests: circuit breaker fires with high loop count, normal critique still fires with low count, tests-failing still takes priority. Structural tests updated for 12 children.

4. **Uses existing infrastructure.** No new types or state — reads `processPatterns.reviewLoopCount` from the assessment, which the shift log parser already computes.

5. **One concern: threshold of 3.** The threshold is hardcoded at 3. The innovation tier uses config for its cap (`maxInnovationCycles`). Consider making the review loop threshold configurable too. However, a hardcoded 3 is reasonable as a starting point — this matches the detection threshold in the shift log parser.

6. **Permitted actions respected.** Only `src/tree/default-tree.ts` and `src/__tests__/default-tree.test.ts` modified.

## Verdict: Clean

## Status

Resolved.
