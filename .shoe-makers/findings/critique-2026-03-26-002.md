# Critique: Commits 1b9b814..a6b59c1

**Reviewer**: adversarial elf
**Date**: 2026-03-26
**Commits reviewed**: 15 commits (setup housekeeping through state file cleanup)

## Summary

This range covers: inbox-driven monolithic file splits (prompts, setup), insight promotion (quorum sensing → smart review-loop-breaker), prompts.test.ts deduplication, claim-evidence.yaml split into 23 files, world.test.ts edge case coverage, and state file tracking fixes.

## Criteria

### 1. Did the elf stay within its permitted files?

**Compliant.**

- `c47e045` (inbox action): wrote `src/prompts/*.ts`, `src/setup/*.ts`, `src/prompts/index.ts` — inbox role permits `src/`, `wiki/`, `.shoe-makers/`.
- `568590e` (execute action — octoclean-fix): wrote `src/__tests__/prompts.test.ts` — executor role permits `src/`.
- `6e08b0c` (evaluate-insight): wrote `.shoe-makers/insights/`, `.shoe-makers/state/`, `.shoe-makers/log/` — permitted.
- `b05bae4` (implement review-loop-breaker): wrote `src/tree/default-tree.ts`, `src/__tests__/default-tree.test.ts` — this was an execute action, executor role permits `src/`.
- `33258ce` (execute — claim-evidence split): wrote `.shoe-makers/claim-evidence/`, `src/verify/parse-evidence.ts`, `src/__tests__/` — executor role permits these.
- `ad011d0` (execute — edge case tests): wrote `src/__tests__/world.test.ts` — executor role permits `src/`.
- `a6b59c1`, `0de0f90` (fix — state file tracking): only `.gitignore` and git operations — housekeeping, acceptable.

### 2. Does the code correctly implement what was asked?

**Compliant.**

- Monolithic file split: `src/prompts/three-phase.ts` and `src/prompts/reactive.ts` now re-export from individual files. Barrel pattern preserves backward compatibility. `src/setup.ts` delegates to `src/setup/*.ts` modules.
- Claim-evidence split: `loadClaimEvidence()` in `src/verify/parse-evidence.ts:106-129` correctly tries the directory first, falls back to single file. Content preserved (2078 lines split vs 2012 original — difference is YAML file headers).
- Smart review-loop-breaker: `src/tree/default-tree.ts:109` adds `review-loop-with-candidates` before the existing `review-loop-breaker`, routing to `prioritise` when candidates exist during a review loop. Logic is correct and deterministic.
- Edge case tests in `world.test.ts` test real behaviors (short hashes, skill-type after line 10, resolved status variants).

### 3. Do the tests actually verify the behaviour, or are they trivial?

**Compliant.**

- `world.test.ts` edge cases: Tests for short hash acceptance, skill-type past line 10 returning null, whitespace trimming, case-insensitive matching, and resolved status variants with trailing punctuation/whitespace. These are substantive edge cases.
- `parse-evidence.test.ts`: New tests verify multi-file loading, single-file fallback, and directory preference. Substantive.
- `default-tree.test.ts`: Updated to verify the new node position and condition logic. Tests both the combined condition (loop + candidates → prioritise) and the fallback (loop without candidates → explore).
- `prompts.test.ts` deduplication: Replaced 5+ ad-hoc factory functions with shared `makeAssessment`/`makeStateWith` helpers. Tests preserved, just less duplicated setup code.

### 4. Were any invariants or evidence patterns modified to game the system?

**Compliant.**

- `.shoe-makers/invariants.md` was not touched (confirmed via `git diff`).
- The claim-evidence split preserved all existing patterns — no evidence rules were added, removed, or altered. The split is structural only.

### 5. Does the change match the wiki spec?

**Advisory finding.** The `review-loop-with-candidates` node (`src/tree/default-tree.ts:109`) adds a new tree node that is not documented in the wiki spec. The wiki's tree order at `wiki/pages/verification.md:124-138` shows:

```
├── [review loop ≥3?]        → Break out to explore
```

But the code now has TWO review-loop nodes: one that routes to `prioritise` when candidates exist, and the original that routes to `explore`. The wiki was not updated to reflect this. This is an enhancement to the tree routing that should be documented.

No wiki files were changed in any of these commits (`git log 1b9b814..HEAD -- wiki/` shows zero results).

## Verdict

**Overall: Compliant with advisory finding.**

All five criteria are met at the blocking level. One advisory issue:

- **Advisory**: `wiki/pages/verification.md` tree order diagram should be updated to include the `review-loop-with-candidates` node. The code diverges from the documented spec. Not blocking because the behavior is correct and tested, but the wiki-as-spec principle requires the spec to stay current.

## Status

Resolved — advisory only, no blocking issues.
