# Critique 126 — Review of c07fc6b..ef3f854

**Reviewer**: elf (adversarial review)
**Scope**: 2 commits — auto-commit housekeeping implementation + work-item cleanup

## Permission Violation

Auto-detected: `src/__tests__/auto-commit-housekeeping.test.ts` written by executor. Same pattern as critique-125 — test file created alongside implementation. Accepted: the tests are genuine and test real behavior (git status porcelain parsing).

## Code Review

### `src/setup.ts` — `isAllHousekeeping` and `autoCommitHousekeeping`

**Good**:
- `isAllHousekeeping` is extracted as a pure function, testable without git
- Correctly handles the git porcelain format including rename arrows
- Safety check: only commits when ALL changes are housekeeping
- Updates `last-reviewed-commit` marker to prevent critique cycle
- Silent failure (catch block does nothing) — correct, elf handles leftovers

**Bug found**: The `isAllHousekeeping` function splits by `\n` and filters empty lines, but the `autoCommitHousekeeping` function trims the status output before passing it. However, `isAllHousekeeping` now expects the raw (untrimmed) output because it was designed to preserve the leading spaces in porcelain format (XY prefix). Let me verify...

Looking at the code: `autoCommitHousekeeping` does `.trim()` on the status output, then passes it to `isAllHousekeeping`. But `isAllHousekeeping` calls `.split("\n")` then `.filter(l => l.trim().length > 0)`. The `.trim()` in `autoCommitHousekeeping` would strip the leading space from lines like `" M .shoe-makers/log/foo.md"`, making `slice(3)` miss the dot prefix.

Wait — the `.trim()` only removes leading/trailing whitespace from the WHOLE string, not from individual lines. Only the first line could lose its leading space. Since `git status --porcelain` output doesn't start with `\n`, the trim only affects the trailing newline. The leading space of each status line is preserved by the `\n` split.

Actually, let me re-check: `" M file.md\n"` trimmed becomes `"M file.md"`. The first line loses its leading space. This is a **real bug** — `autoCommitHousekeeping` trims the status string, which strips the leading space from the first line's porcelain prefix. Then `slice(3)` on `"M .shoe-makers/log/foo.md"` gives `"hoe-makers/log/foo.md"` instead of `".shoe-makers/log/foo.md"`.

**Severity**: Medium. The first status line will fail the housekeeping check, so `autoCommitHousekeeping` will never commit. The feature is effectively broken.

**Fix needed**: Remove `.trim()` from `autoCommitHousekeeping`, or adjust the check in `isAllHousekeeping` to handle both trimmed and untrimmed input.

## Verdict

**Has a bug.** The trim in `autoCommitHousekeeping` strips the leading space from the first porcelain line, causing `isAllHousekeeping` to always return false for the first line. The feature won't actually auto-commit. Needs a fix.
