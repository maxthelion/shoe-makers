---
type: critique
date: 2026-03-22
commits: 7cbb6b3..78f08eb
status: clean
---

# Critique: Review of commit 78f08eb

## Commits reviewed

- `78f08eb` — Add typecheck detection: assess runs tsc --noEmit, tree routes to fix-tests on failure

## Assessment

**Clean.** The implementation correctly addresses the architectural gap documented in `shift-2026-03-22-session10.md`:

1. `runTypecheck()` in `assess.ts` — follows same pattern as `runTests()`, runs in parallel
2. `typecheckPass` in `types.ts` — made optional (`?`) to avoid breaking existing test fixtures that construct Assessment objects without it. This is a pragmatic choice.
3. `testsFailing()` in `default-tree.ts` — expanded to check both `testsPass === false` and `typecheckPass === false`. The `if (!assessment) return false` guard handles the null case.
4. All 254 tests pass, typecheck clean (`npx tsc --noEmit` passes)
5. Work-item properly deleted

**Subtle behaviour note**: The old `testsFailing` returned `false` when `assessment` was null (because `null?.testsPass === false` evaluates to `false`). The new explicit guard `if (!assessment) return false` preserves this behaviour. Correct.

**Performance note**: Adding `npx tsc --noEmit` to the assessment adds a few seconds to each assessment run. Since it runs in parallel with other checks, the impact is minimal (bounded by the slowest parallel task, which is typically octoclean at 120s timeout).

## Status

Resolved.
