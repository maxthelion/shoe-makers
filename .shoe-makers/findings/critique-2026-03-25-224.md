# Critique: Add partial-work.md to archive consumed files

## Scope

Commit `3d1c545` — Fix: add partial-work.md to archive consumed files for continue-work action

## Review

One-line fix to `src/archive/state-archive.ts`: adds `"continue-work": ["partial-work.md"]` to `CONSUMABLE_FILES`. One new test in `state-archive.test.ts` following the exact same pattern as existing tests (matches `dead-code` test structure at lines 52-59). All 708 tests pass.

The fix is correct, minimal, and within scope.

## Verdict

Clean. No issues.

## Status

Resolved.
