# Critique: ddb5168..0a820dd

**Reviewer**: adversarial-review elf
**Date**: 2026-03-23
**Commits reviewed**: 22 commits (3 substantive, rest housekeeping/logging)
**Previous elf's action**: Adversarial Review (same action — review chain)

## Substantive changes

### 1. `expectPromptContains` helper refactor (88ab6cc)

**Verdict: Good refactor, minor concern.**

The helper correctly reduces boilerplate from 459 to 393 lines without changing test semantics. Each original `generatePrompt` + `expect().toContain()` call maps cleanly to the helper.

Minor concern: The helper doesn't accept the `article` parameter for `generatePrompt`, so the creative-lens tests (line ~278) still call `generatePrompt` directly. This is fine but creates two patterns for the same operation. Not a bug, just an inconsistency that could confuse a future elf.

### 2. Morning review dashboard (0e4f0cf, bd0e644)

**Verdict: Solid implementation, one latent bug.**

`formatDashboard` and `prependShiftDashboard` are well-structured. The idempotency approach (regex-strip then re-insert) is sound. Tests cover the key cases: insertion, idempotency, and no-file-exists.

**Bug — DASHBOARD_PATTERN anchoring**: The regex `DASHBOARD_PATTERN = /\n> \*\*Shift Dashboard\*\*[^\n]*\n> [^\n]*/g` requires a leading `\n` before the dashboard block. In `prependShiftDashboard`, the dashboard is inserted as `header + "\n" + dashboard + cleaned`. This means the dashboard text starts at position `header.length + 1` with no preceding newline character before the `>`. The pattern will match on subsequent calls because the `body` (which starts at `firstNewline`) will contain `\n> **Shift Dashboard**...` — but only because the `\n` in the pattern matches the newline that was *part of the previous insertion*. This works by accident: the `\n` in the pattern matches the newline that `header + "\n"` creates. If the format ever changes (e.g., adding a blank line after header), the strip would silently fail and dashboards would accumulate. The test passes because the current format is consistent, but the coupling is fragile.

**Test coverage**: The `prependShiftDashboard` test at line ~275 checks `afterHeader` contains the dashboard marker, but doesn't verify the *full content order* (header, dashboard, entries). A stronger test would check the relative positions.

### 3. Housekeeping commits

The explore/prioritise/cleanup commits are routine state machine transitions. No issues.

## Overall Assessment

Good work. The refactor is clean and the dashboard feature is well-tested. The DASHBOARD_PATTERN fragility is a real concern but not currently broken — it would only manifest if the log format changes.

## Status

Resolved. On closer inspection, the DASHBOARD_PATTERN works by design: `body = existing.slice(firstNewline)` always starts at the `\n` character, so the leading `\n` in the regex correctly anchors against it. The fragility concern was overstated — the coupling is intentional and consistent.
