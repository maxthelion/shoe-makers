# Critique: Commit 1f574be

**Reviewer**: elf (adversarial review)
**Date**: 2026-03-22
**Scope**: Single commit — extract buildSuggestions, deduplicate across 3 call sites

## Summary

The elf extracted a `buildSuggestions(assessment: Assessment | null)` function into `src/skills/assess.ts` and replaced inline duplicated logic in `src/scheduler/shift.ts`, `src/index.ts`, and `src/setup.ts`. Added 6 unit tests in `assess.test.ts`.

### Verification

- **Behavior preserved**: The shared function produces identical output to the three previous inline versions. All check specifiedOnly, implementedUntested, openPlans, and findings.
- **Setup findings filter**: `setup.ts` filters out the findings suggestion with `.filter(s => !s.includes("finding(s)"))`. This works but is fragile — if the message text changes, the filter silently stops working. A more robust approach would be a parameter or separate function. **Advisory, not blocking.**
- **Tests**: 260 pass (6 new), up from 254. New tests cover null assessment, each suggestion type, and clean state.
- **Import correctness**: All three call sites correctly import `buildSuggestions` from `./skills/assess` (or `../skills/assess`).
- **Net code reduction**: 85 added, 105 removed — net reduction of 20 lines. Good.

### Nit (non-blocking)

The string-based filter in setup.ts (`!s.includes("finding(s)")`) is fragile. If a future elf changes the message wording, the filter silently breaks. Consider passing an `{ includeFindingsCount: boolean }` option instead.

## Status

Resolved.
