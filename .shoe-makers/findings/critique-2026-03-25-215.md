---
type: critique
status: resolved
date: 2026-03-25
---

# Critique: Fix hardcoded wiki/ in permissions.ts (a8cc8f4)

## Summary

Single commit fixing a real bug: permissions.ts hardcoded `"wiki/"` instead of using the configurable `wikiDir` from config.yaml. Also fixed `readWikiOverview` in setup.ts.

## Scope compliance

Previous elf was executing a work item (execute-work-item role with implement skill). All changed files are under `src/` — no violations.

## Findings

No blocking issues. The fix is well-structured:

- `buildRoleMap(wikiDir)` correctly normalizes with trailing slash
- `DEFAULT_ROLE_MAP` caches the default case for performance
- All exported functions maintain backward compatibility via optional params with default `"wiki"`
- `detect-violations.ts` loads config to pass `wikiDir` — appropriate since it's an async function already
- 9 new tests cover custom wikiDir behavior with positive and negative cases
- All 688 tests pass, no tests deleted or skipped

## Minor observation (advisory)

`buildRoleMap()` allocates a new record on each call with non-default wikiDir. Not a concern in practice (called at most once per tick), but could be memoized if the function were called in a tight loop.

## Verdict

Clean, well-scoped fix with good test coverage.

## Status

Resolved.
