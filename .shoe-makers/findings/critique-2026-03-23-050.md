# Critique: 8a8704c..6f820d3

**Reviewer**: Elf (adversarial review)
**Commits reviewed**: ~45 commits (8a8704c..6f820d3)
**Date**: 2026-03-23

## Summary of Changes

Five major work items were executed:

1. **Dead code removal** (60d1092): Deleted `src/skills/prioritise.ts`, `src/skills/work.ts`, and their test files (`prioritise.test.ts`, `work.test.ts`)
2. **Init-skill-templates split** (ccd1e0f): Split monolithic `init-skill-templates.ts` into three files by category: `-work.ts`, `-quality.ts`, `-docs.ts`
3. **EXCLUDED_TOP_LEVEL fix** (0e7cc60, 4d78594): Updated the exclusion set in `invariants.ts` to reflect the template file split, and added `utils` directory
4. **Doc sync** (78ae0e0): Updated `wiki/pages/integration.md` skill list to include all 9 skills
5. **Evaluate.test.ts refactor** (47a2f1d): Parameterized routing and priority tests, reducing 324→236 lines

## Assessment

### Dead Code Removal — Clean

The removal of `prioritise.ts` and `work.ts` is justified. These were bootstrap implementations superseded by the skill-based system. The commit message correctly notes 0 callers and 496 tests still passing. Deleting their test files is appropriate — the tests tested removed features.

### Init-Skill-Templates Split — Clean

The split is mechanical and correct. Each template string moved to its categorically appropriate file. Import paths in `init.ts` and `init-templates.test.ts` were updated correctly. No template content was altered during the move. Good.

### EXCLUDED_TOP_LEVEL Fix — Clean, One Minor Observation

The fix correctly adds the three new filenames (`init-skill-templates-work.ts`, `-quality.ts`, `-docs.ts`) and removes the old `init-skill-templates.ts`. Adding `"utils"` as a directory exclusion is also correct.

However, the old entry `"init-skill-templates.ts"` was left in the exclusion set — it's now stale since that file was renamed. This is harmless (it just means a non-existent file won't trigger "unspecified" warnings), but it's dead data. Very minor.

### Doc Sync — Clean

The `integration.md` update correctly lists all 9 skill names. Accurate.

### Evaluate.test.ts Refactor — Mostly Clean, One Concern

The refactoring from individual test cases to parameterized `routingCases` and `priorityCases` arrays is a reasonable DRY improvement. The helper `failingTestsBlackboard()` reduces boilerplate.

**Concern**: The original test suite had a test explicitly verifying the full priority chain: "priority order: tests > critiques > reviews > uncommitted > inbox > work-item > candidates > explore". The refactored version only tests pairwise priorities (fix-tests over review, work-item over candidates, inbox over work-item, fix-tests over everything). This is a slight reduction in test specificity — the original explicitly named the full ordering in the test name and verified the top priority when *everything* is active. The refactored version has a "fix-tests over everything" case that covers the same scenario, but the explicit full-chain documentation in the test name is lost.

This is a style concern, not a bug — the same behaviour is tested. But the original was more self-documenting.

### Shift Log / Explore / Prioritise Commits — Administrative

The numerous "Update shift log" and explore/prioritise commits are mechanical scheduler output. No concerns.

## Verdict

**Clean shift.** All changes are correct, tests pass (496/496), and the work follows spec. The two minor observations (stale exclusion entry, slightly less self-documenting test names) are cosmetic and don't warrant action.

## Status

Resolved — no issues requiring follow-up.
