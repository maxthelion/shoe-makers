# Critique: Commits d21d4c5..2108725

**Reviewer**: elf (adversarial review)
**Date**: 2026-03-22
**Scope**: 4 substantive commits — YAML parser extraction, claim extraction, config validation, typecheck logging

## Overview: Clean — minor nits only

## Summary

The previous elf made four changes:

1. **caae6dd** — Extract YAML parser from `invariants.ts` into `parse-evidence.ts`
2. **a92204b** — Add config validation warning for unknown keys
3. **b28c524** — Extract claim extraction from `invariants.ts` into `extract-claims.ts`
4. **f73384b** — Log typecheck result in setup assessment output

Plus several explore/prioritise/session-log housekeeping commits.

## Findings

### Good

- **Refactors are behavior-preserving**: Both extractions (parse-evidence.ts and extract-claims.ts) are pure code moves. The functions are identical to the originals. No logic was changed.
- **Re-exports maintain backward compatibility**: `invariants.ts` re-exports the moved types and functions so existing imports don't break.
- **All 254 tests pass** with 0 failures.
- **Config validation is non-breaking**: Unknown keys produce a `console.warn`, not an error. This is the right call.
- **Typecheck logging is guarded**: Only logs when `typecheckPass !== undefined`, avoiding noise when typecheck isn't run.

### Nits (non-blocking)

1. **Redundant import + re-export pattern in invariants.ts**: Lines 4-5 import `EvidenceRule` and `Claim` as types for local use, while lines 8-9 re-export them. The re-export alone would suffice for both local use and external consumers — the separate import is unnecessary. Not a bug, just slightly noisy.

2. **KNOWN_KEYS in load-config.ts is a maintenance burden**: The hardcoded Set of known keys will silently fall out of sync if new config keys are added to the DEFAULTS object but not to KNOWN_KEYS. There's no test or static check that KNOWN_KEYS matches the keys recognized by the parser. Low risk now, but could cause false warnings later.

### No issues found with

- No architectural violations — all changes are pure refactors or additive logging
- No cheating — no test modifications
- No spec misalignment — these are internal code quality improvements
- No side effects in agents
- No evidence pattern loosening

## Status

Resolved.
