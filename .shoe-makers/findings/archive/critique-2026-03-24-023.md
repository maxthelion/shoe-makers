# Critique — 2026-03-24-023

## Scope
Commits since last review: consolidate housekeeping path constants + prioritise commit.

## Findings
### Clean: well-executed consolidation
`CORE_HOUSEKEEPING_PATHS` (private) defines the 3 shared paths once. `SETUP_HOUSEKEEPING_PATHS` (for violation detection) is an alias — no `.shoe-makers/state/`. `ALL_HOUSEKEEPING_PATHS` (for auto-commit) extends with state/. The JSDoc comments clearly explain why each variant exists.

### Clean: backward compatibility preserved
`setup.ts` still exports `HOUSEKEEPING_PATHS` (now set to `ALL_HOUSEKEEPING_PATHS`), so existing test imports don't break. 664 tests pass.

### Clean: tests verify the key invariant
Tests confirm ALL is a superset of SETUP, and that state/ is only in the extended set. This is exactly the contract that needs protecting.

## Verdict
**Clean.** Mechanical refactor, correct, well-documented.

## Status

Resolved.
