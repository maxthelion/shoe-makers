# Critique — 2026-03-24-017

## Scope
Commit 1e9d961: Fix fragile checkUnreviewedCommits test.

## Findings
### Clean: correct fix for a real fragility
The old test used `HEAD~5` assuming there'd be a non-housekeeping commit in that range. After several consecutive housekeeping/critique commits, all 5 were filtered out by `getElfChangedFiles`, causing the test to fail.

The fix dynamically finds the most recent commit touching `src/` (guaranteed non-housekeeping) and sets the marker to its parent. This is robust against any sequence of housekeeping commits.

### Minor: edge case if srcCommit is the root commit
If `git log --format=%H -1 -- src/` returns the very first commit, `git rev-parse <hash>~1` would fail. This is a theoretical concern only — the repo has hundreds of commits.

## Verdict
**Clean.** The fix addresses the root cause correctly. No tests deleted or weakened. 657 tests pass.

## Status

Resolved.
