# Critique — 2026-03-24-026

## Scope
Commit 333694f: Unify frontmatter parsing into shared utility module.

## Findings
### Clean: correct extraction and good test coverage
New `parseFrontmatter()` and `getFrontmatterField()` utilities replace three different regex patterns. All three consumers updated correctly:
- `extract-claims.ts` — now uses shared utility via aliased import
- `registry.ts` — replaced inline regex, kept field-loop parsing
- `assess.ts` — replaced regex with explicit `getFrontmatterField()` calls for category/status

10 new tests (6 for parseFrontmatter, 4 for getFrontmatterField) covering standard cases, edge cases, and missing frontmatter. 674 total tests pass.

### Observation: assess.ts is now stricter
The old regex `/category:\s*plan/` would match `category: planning` or `subcategory: plan`. The new code `getFrontmatterField(..., "category") !== "plan"` is an exact match. This is actually *better* — the old behavior was overly loose. Verified all 20 wiki pages parse correctly with the new utility.

### Note: 4 specified-only invariants appeared
The invariant count changed from 0 to 4 specified-only after this commit. This appears to be a pre-existing condition rather than a regression from this change, since the frontmatter parsing for invariant extraction (in extract-claims.ts) now uses the same utility and all wiki pages parse identically.

## Verdict
**Clean.** Well-executed consolidation with improved strictness.

## Status

Resolved.
