---
type: critique
date: 2026-03-22
commits: 6e6d1f5..6ba96e5
status: clean
---

# Critique: Review of commit 6ba96e5

## Commits reviewed

- `6ba96e5` — Prioritise: pick dead-code action type wiring, write work-item.md

## Assessment

**Clean with one advisory note.** The work-item.md is detailed and specific, with inline code examples for each change. Correctly identifies that dead-code doesn't need its own tree node — it routes through execute-work-item.

**Advisory**: The work-item says to add `"dead-code": "dead-code"` to `ACTION_TO_SKILL_TYPE` in prompts.ts. But the executor (execute-work-item action) is what currently invokes work items. For the dead-code skill prompt to be injected, either: (a) the behaviour tree needs a separate `dead-code` condition/action node that fires when a work-item has type dead-code, OR (b) the execute-work-item prompt generator needs to detect the work-item's skill type and use it. Currently `findSkillForAction` looks up the action type, not the work-item content. The executor should verify how skill selection works during execution.

No source files modified. Candidates consumed. Work-item written.

## Status

Resolved.
