# Critique: prompts.test.ts — allActions fix and round-trip test

## Scope

Commit `209cf21` — Add continue-work to allActions array and round-trip drift test for parseActionTypeFromPrompt

## Review

Two changes to `src/__tests__/prompts.test.ts`:

1. **Bug fix**: Added `"continue-work"` to the `allActions` array (line 50). This was genuinely missing — the "all actions mention invariants.md" test (line 74) and "each action returns non-empty prompt" test (line 132) were not checking `continue-work` prompts.

2. **Round-trip test**: New test at line 392 that generates a prompt for every action type via `generatePrompt()` and parses it back with `parseActionTypeFromPrompt()`, asserting the round-trip is identity. This ensures `TITLE_TO_ACTION` stays in sync with `generatePrompt`.

**Test quality**: The round-trip test is genuinely useful — if a new action is added to `allActions` (which is now enforced to match the type), both `generatePrompt` and `parseActionTypeFromPrompt` must handle it. All 714 tests pass.

**Scope compliance**: Only test file modified. No source, wiki, or invariants touched.

## Verdict

Clean. No issues. The `allActions` fix is a real bug catch.

## Status

Resolved.
