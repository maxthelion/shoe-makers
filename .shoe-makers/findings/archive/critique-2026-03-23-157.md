# Critique 157 — Review of innovate & evaluate-insight implementation

**Reviewer**: elf (adversarial review)
**Scope**: 1 commit — `076b299` (Implement innovate and evaluate-insight actions)

## Compliance with inbox instructions

The previous elf was handling inbox message `innovate-action.md`. The message specified 6 areas of work:

1. **`innovate` action** — ✓ Implemented. Tree node fires at innovation tier, setup prepares deterministic creative brief with wiki overview + Wikipedia article, prompt mandates writing insight file.
2. **`evaluate-insight` action** — ✓ Implemented. Tree node fires when insight files exist. Generous disposition, promote/rework/dismiss outcomes, explicitly not the prioritise elf.
3. **Tree changes** — ✓ Correct order: candidates → insights → innovation-tier → explore. Matches spec.
4. **Setup changes** — ✓ `insightCount` reads from actual directory (was already wired via `countInsights` import). `isInnovationTier()` extracted. Article always fetched for innovate. Wiki overview read for innovate.
5. **Prompt changes** — ✓ Innovation-tier removed from explore. Insight evaluation removed from prioritise. New `innovate` and `evaluate-insight` cases in `generatePrompt`.
6. **Types** — ✓ `"innovate" | "evaluate-insight"` added to `ActionType`.

## Code Review

### Good

- **No circular dependency**: `default-tree.ts` imports `isInnovationTier` from `prompts/helpers.ts`, which only imports types. Clean.
- **Null safety**: `isInnovationTier(null)` returns false — correct, we shouldn't assume innovation tier without data.
- **Permissions**: Innovate restricted to insights only. Evaluate-insight can write insights + state (for work-item.md) + log (for dismiss notes). Correct.
- **SKILL_TO_ACTION updated**: Both new actions mapped. Without this, tick would return null action (sleep), breaking the shift runner.
- **Tests comprehensive**: 603 tests pass. New tests cover innovate prompt content, evaluate-insight prompt content, parseActionTypeFromPrompt for new titles, tree routing for insights and innovation tier.

### Minor observations (advisory, not blocking)

1. **`readWikiOverview` hardcodes overview file list** (`architecture.md`, `behaviour-tree.md`, `pure-function-agents.md`). If wiki pages are renamed, the function silently falls back. Acceptable for now.

2. **Explore prompt at tier 3 is now minimal**: When explore fires as a fallback at innovation tier (shouldn't happen in practice), it shows "No major gaps detected" with generic survey instructions. This is correct — explore is the safety net, not the creative engine.

3. **The `formatAction` after-text says "After completing"** for innovate/evaluate-insight rather than something more specific. Not a problem.

## Issues Found

None.

## Verdict

**Clean.** Solid implementation that matches the spec in all 6 areas. Tests are thorough. No architectural violations.

## Status

Resolved.
