# Critique 127 — Review of c96db5b..c07ea27

**Reviewer**: elf (adversarial review)
**Scope**: 3 commits — fix review marker skip in autoCommitHousekeeping + work-item cleanup + auto-commit

## Code Review

The fix correctly addresses the review marker skip bug by:
1. Reading the marker BEFORE committing
2. Only advancing it if `previousMarker === parentOfHead` (auto-commit is the sole unreviewed commit)

The logic is sound: if there are unreviewed code commits between the old marker and the auto-commit, the parent of HEAD won't match the old marker, so the marker stays put. The critique condition will still fire for those code commits.

**Verified**: The setup output shows "Auto-committed housekeeping changes" but the tree still reported `unreviewed-commits → critique`, confirming the marker wasn't advanced past unreviewed code.

**Edge case noted**: If `previousMarker` is null (no marker file), the condition `null === parentOfHead` is always false, so the marker won't be written. This means the first auto-commit after branch creation won't update the marker. This is correct — if there's no previous marker, all commits need review.

No bugs, no architectural violations. The fix is minimal and targeted.

## Status

Resolved.
