# Critique: Typecheck Fix + Critique Format Fix (2026-03-25)

**Reviewed commits:** 4134717..4f2bbd3 (2 code commits + housekeeping auto-commits)

## Commits Reviewed

- `b1018d0` — Fix critique status format to match RESOLVED_PATTERN
- `a7a4437` — Fix typecheck errors in wikipedia tests with non-null assertions

## Findings

### 1. Non-null assertions in wikipedia.test.ts (a7a4437) — CLEAN

The fix adds `!` non-null assertions on three `result` variables passed to `toContainEqual()`. Each is preceded by `expect(result).not.toBeNull()` which validates non-nullness at runtime. The `!` assertion tells TypeScript what the test already verifies. This is the correct, minimal fix — no tests deleted, no logic changed, root cause addressed.

### 2. Critique status format fix (b1018d0) — CLEAN

Changed the status line format in the archived critique to match the RESOLVED_PATTERN regex. This is a mechanical fix to a findings file, well within scope.

### 3. Compliance

Both commits are focused, minimal, and address only the identified problems. No off-limits files were modified.

## Verdict

No issues found. Both changes are clean and correct.

## Status

Resolved.
