# Critique 074 — Review explore commit 58589bd

## Commits reviewed

- `58589bd` Explore: write 5 candidates — review oscillation root causes

## Scope

Explore action output: `.shoe-makers/state/candidates.md` with 5 ranked work items.

## Assessment

**Good work overall.** The candidates correctly identify the root cause of high reactive ratio (review oscillation from subjective criteria) and propose architectural fixes rather than surface patches.

### Strengths

1. All 5 candidates reference specific file paths and code locations
2. The analysis connects process signals (65% reactive ratio) to specific code in `src/prompts/reactive.ts` and `src/tree/default-tree.ts`
3. Candidate #3 correctly identifies the `RESOLVED_PATTERN` fragility at `src/state/world.ts:103` — this was demonstrated empirically during the same session
4. Candidates are properly typed with skill references matching available skills

### Minor issues

1. **Line number inaccuracy in candidate #5**: Claims `src/setup.ts` lines 200-208 for the marker advancement logic, but the actual marker check (`previousMarker === parentOfHead`) is at lines 220-232. The description of the logic is correct, just the line references are off.

2. **Candidate #1 Type mismatch**: Listed as `health` type but the work involves modifying prompt templates (`src/prompts/reactive.ts`), not improving code health scores. Better typed as `improve` or `health` is a stretch — the available types include "health" for octoclean-style improvements, not prompt engineering.

3. **Missing invariant IDs**: The explore prompt asks to "reference file paths, wiki pages, and invariant IDs." Candidates reference file paths and wiki pages but no specific invariant IDs from `.shoe-makers/invariants.md`.

### No compliance violations

- No source code, wiki, or test files modified (only `candidates.md` and shift log)
- 5 candidates produced (meets the 3-5 requirement)
- No off-limits files touched

## Verdict

Compliant with minor line-reference inaccuracy. Candidates are substantive and actionable.

## Status

Resolved.
