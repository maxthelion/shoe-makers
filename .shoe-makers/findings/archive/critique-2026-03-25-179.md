# Critique — 2026-03-25 Session (Fix Tests)

## Commits reviewed

- `a606bfb` Fix health-scan integration tests when octoclean is not installed
- `3ce3643` Fix runTypecheck to return null when type definitions are missing

## Verdict: Acceptable with minor concerns

### health-scan.test.ts change

**Approach**: When `octoclean` is not installed, the integration tests now assert `null` instead of expecting a score. No tests were deleted or skipped — good.

**Concern — hardcoded path check**: The test checks for `node_modules/octoclean/src/cli/index.ts` specifically. If octoclean changes its entry point path, the check would be stale. However, this matches the exact path used in `health-scan.ts` line 21, so it's consistent. Low risk.

**Concern — test still validates nothing when octoclean is absent**: When `octocleanAvailable` is false, the test just confirms `getHealthScore` returns `null` — which is already covered by the "nonexistent-repo" test above. The test becomes a near-duplicate in that case. This is acceptable but worth noting: these tests only add value when octoclean is installed.

### assess.ts change (runTypecheck)

**Approach**: Distinguishes "tsc can't run due to missing type definitions" from "code has actual type errors" by inspecting error output. Returns `null` for environment issues instead of `false`.

**Concern — string matching fragility**: The check `output.includes("Cannot find type definition file")` depends on the exact wording of a TypeScript compiler error message. If TypeScript changes this message in a future version, the function would revert to returning `false` for environment issues. This is a reasonable trade-off for now but could cause false "tests failing" routing in the future.

**Positive**: The return type change to `boolean | null` aligns with the existing `typecheckPass?: boolean | null` type definition, and the tree condition `typecheckPass === false` correctly ignores `null`. No type system abuse.

## No off-limits violations detected

The elf only modified files referenced by failing tests and the source under test. No tests were deleted or skipped.

## Status

Resolved.

Minor concerns acknowledged — hardcoded path matches the source, and string matching is a reasonable trade-off given TypeScript's stable error messages. No code changes needed.
