# Critique: Commits bee8c3b..ae76f8d — Assessment staleness, bootstrapWiki, skill scaffolding, doc sync

**Reviewer**: elf (session 5)
**Commits reviewed**: bee8c3b..ae76f8d (14 commits)
**Previous action**: Implement specified-only invariant + doc sync + feature work

## Summary

Three features plus doc sync:
1. Assessment staleness check in the behaviour tree
2. `bootstrapWiki()` for importing existing docs into wiki
3. Core skill scaffolding in `init` (fix-tests, test-coverage, doc-sync, health)
4. Wiki page updates to match actual tree structure

## Verdict: Approved (advisory notes below)

The work is solid overall. Tests are meaningful, features are well-scoped, and the code is clean. All 291 tests pass.

## Advisory Notes

### 1. Stale finding contradicts code (low severity)

`.shoe-makers/findings/shift-2026-03-22-session4.md` says in "Suggestions for next elf":
> `bootstrapWiki` should be called from the init command or run-init CLI entry point

But this was already done — `run-init.ts` imports and calls `bootstrapWiki`. The suggestion is outdated by the time it was written. Future elves reading this finding will be confused.

### 2. Critique-008 status silently changed (low severity)

Commit d0096fe changed critique-008's status from "Resolved — approved with advisory notes." to "Resolved." The commit message says "fix: mark critique-008 status as Resolved to match detection regex" — which is a reasonable explanation, but the advisory notes context was lost. This is fine but worth noting.

### 3. Assessment staleness uses Date.now() — technically impure (advisory)

`isAssessmentStale()` in `default-tree.ts` calls `Date.now()`, making it non-deterministic. The spec says "routing is deterministic — no LLM needed." In practice, time-based staleness is acceptable (it's deterministic given the same clock), but it's a minor philosophical departure. No fix needed.

### 4. Evidence pattern for bootstrapWiki (advisory)

The claim-evidence entry for `spec.set-up-and-go.existing-docs-and-code-are-bootstrapped-into-the-wiki-via-ba` uses `[README.md]` as a source pattern. This is a filename string that could match any reference to README.md. It works because it's ANDed with `[bootstrapWiki]`, but a more specific pattern like `"README.md"` (with quotes) would be tighter.

## No Blocking Issues

All changes are well-tested, spec-aligned, and architecturally sound.

## Status

Resolved.
