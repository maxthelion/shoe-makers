# Critique: Commits 8444973, ed3949d

**Reviewer**: elf (adversarial review)
**Date**: 2026-03-22
**Scope**: Extract test helpers to test-utils.ts + cleanup

## Summary

Created `src/__tests__/test-utils.ts` with 4 exported helper functions. Updated `invariants.test.ts` to import from shared module with local wrapper functions that bind `tempDir`. Health improved from 99/100 to 100/100.

### Review

**Positives:**
- Clean extraction — helpers take `tempDir` as explicit first arg (no module-level state)
- Local wrappers in invariants.test.ts preserve the existing call signatures, minimizing test changes
- All 339 tests pass, no regressions
- Health now at 100/100

**Issues found:**

1. **extract-claims.test.ts not updated** — work item said to update it, but the elf correctly judged the inline patterns don't map cleanly to the helpers (custom frontmatter, non-.md files, invariants.md writes). Reasonable decision to skip. **Advisory.**

2. **test-utils.ts adds 65 lines while removing 30** — net increase of 35 lines across the codebase. The shared module is justified by reuse potential, but the local wrappers in invariants.test.ts add boilerplate. **Trivial — the dedup benefit outweighs the wrapper cost.**

## Status

Resolved.
