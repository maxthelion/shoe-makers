---
type: critique
date: 2026-03-22
commits: b3c1d5e..1329c48
status: mostly-clean
---

# Critique: Review of commit 1329c48

## Commits reviewed

- `1329c48` — Wire dead-code action type into behaviour tree, permissions, and prompts

## Assessment

**Mostly clean.** The implementation correctly adds `"dead-code"` to all required locations:

1. `src/types.ts` — ActionType union ✓
2. `src/verify/permissions.ts` — ROLE_MAP with `canWrite: ["src/"]` (includes `src/__tests__/`) ✓
3. `src/scheduler/tick.ts` — SKILL_TO_ACTION map ✓
4. `src/prompts.ts` — ACTION_TO_SKILL_TYPE and switch case ✓
5. `src/scheduler/run-skill.ts` — switch case ✓
6. `src/log/shift-summary.ts` — ACTION_TO_CATEGORY ✓

All 260 tests pass. The assertion count increased from 706 to 708, indicating existing exhaustive tests picked up the new action type.

### Advisory: No tree node for dead-code

The `dead-code` action type is wired into permissions and prompts but has no condition in `src/tree/default-tree.ts`. The behaviour tree won't ever route to `dead-code` — it will always route through `execute-work-item` when a work-item.md exists. For dead-code to be used, the setup/scheduler would need to detect that the current work-item calls for dead-code and override the action type. This is a gap — the new action type is registered but unreachable through the normal tree evaluation path.

### Advisory: No test for dead-code permissions

The executor TDD enforcement tests verify that `execute-work-item` can't write tests, but there's no test verifying `dead-code` CAN write tests. The existing permission tests use an exhaustive action type loop, so the new type is covered by those, but a specific test would be better.

## Status

Resolved.
