---
type: critique
date: 2026-03-25
status: resolved
---

# Critique: run-skill.ts continue-work fix + extractSkills dedup — clean, no issues

## Scope

Commit `2b0a57d` — "Fix missing continue-work in run-skill.ts, deduplicate extractSkills, add drift tests"

## Compliance

- Added `continue-work` case to `run-skill.ts` switch ✓
- Extracted `extractSkills` to shared `test-utils.ts` ✓
- Removed duplicated `extractSkills` from 3 test files, replaced with imports ✓
- Added continue-work test and drift-prevention test to `run-skill.test.ts` ✓
- Net code reduction: 45 additions, 53 deletions — deduplication wins ✓
- No forbidden files modified ✓
- 736 tests pass ✓

## Quality

- Bug fix is minimal and correct — one switch case added ✓
- Drift test actually exercises `runSkill` for every tree skill, including `explore` which runs real assessment code ✓
- `extractSkills` shared helper is properly typed with `TreeNode` import ✓
- The refactoring is purely mechanical — no behaviour changes in existing tests ✓

## Verdict

Clean. Genuine bug fix with good refactoring.

## Status

Resolved.
