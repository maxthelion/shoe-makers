# Critique — 2026-03-24-029

## Scope
Commits since last review: fix plan-status invariant evidence regression + prioritise commit.

## Findings
### Clean: evidence patterns correctly updated
Added alternative patterns to 4 claim-evidence entries to match the refactored code:
- `=== "blocked"` and `=== "done"` for status checking
- `"category") !== "plan"` for category checking

These patterns match the actual source code after comment stripping. All 221 invariants now pass as implemented-tested. 674 tests pass.

### Note: evidence brittleness
This was caused by the evidence pipeline doing simple string matching on source code. Any refactoring that changes the literal strings (even while preserving semantics) can break evidence. This is a known limitation — the evidence patterns are intentionally simple. The fix of adding alternative patterns is the correct approach.

## Verdict
**Clean.** Regression fixed, all invariants satisfied.

## Status

Resolved.
