# Critique — 2026-03-24-021

## Scope
Commit c4a1957: Explore — write candidates for consolidating housekeeping paths, unifying frontmatter parsing, dead code removal.

## Findings
### Clean: well-researched candidates with concrete evidence
Four candidates, all with specific file paths and line numbers. The top candidate (consolidating HOUSEKEEPING_PATHS) correctly identifies a subtle difference between the two definitions — `SETUP_HOUSEKEEPING_PATHS` has 3 paths while `HOUSEKEEPING_PATHS` has 4 (includes `.shoe-makers/state/`). The analysis explains why the difference exists and how to consolidate safely.

### Note: candidate #3 (dead code) needs careful evaluation
The "unused" exports `evaluate()` and `findSkillForType()` are used in tests. Removing them could break test patterns. The candidate correctly flags this but should be weighed carefully during prioritisation — test-only exports aren't truly dead.

## Verdict
**Clean.** Good exploration with actionable findings.

## Status

Resolved.
