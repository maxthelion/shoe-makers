# Critique 078 — Review execute commit 6aeb742

## Commits reviewed

- `6aeb742` Tighten loose claim-evidence patterns to reduce false-positive matches

## Criteria assessment

1. **Permitted files**: Compliant. Only `.shoe-makers/claim-evidence.yaml` modified (plus housekeeping archive/log).

2. **Correctly implements what was asked**: Compliant. Work item asked to tighten loose single-keyword patterns. 12 patterns were tightened by adding multi-word alternatives or function-specific terms. Key changes:
   - `[children]` → `["node.children", "child of node.children"]` — matches actual code pattern
   - `[branch]` → `[getCurrentBranch, branchName]` or `[branchPrefix, "shoemakers/"]` — matches function names
   - `[action]` → `[ActionType, "evaluate("]` — matches actual type names
   - `[findings]` → `[readFindings, findings]` — adds function name specificity

3. **Tests verify behaviour**: Compliant. All 26 invariant tests pass after changes (verified by running `bun test src/__tests__/invariants.test.ts`). Full test suite passes (765 pass, 0 fail). The assessment now reports 1 "untested" claim — this means the tightening correctly exposed a gap where test evidence patterns were too loose, which is the intended outcome.

4. **No invariant/evidence gaming**: Compliant. No new claims added, no claims removed. Patterns were only tightened (made more specific), not loosened.

5. **Matches wiki spec**: Compliant. The wiki (`wiki/pages/invariants.md`) describes evidence patterns as needing to verify actual behaviour. Tighter patterns better match this intent.

## Note on the new untested claim

The assessment now shows 1 `implementedUntested` claim. This is correct — one of the tightened test patterns no longer matches against test code, exposing a genuine coverage gap. This should be addressed in a future test-coverage work item, not masked.

## Verdict

Clean. All criteria compliant.

## Status

Resolved.
