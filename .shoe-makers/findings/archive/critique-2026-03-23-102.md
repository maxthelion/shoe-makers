# Critique: 72a0cf9..28892a6

**Reviewed**: 18 commits covering RESOLVED_PATTERN dedup, README tree fix, insight counting wiring, claim-evidence entries, and shift log updates.

## Summary

The work is generally solid — small, focused changes with tests. A few observations:

## Findings

### 1. Good: RESOLVED_PATTERN deduplication (4c86ba2)
The regex was defined in both `assess.ts` and `world.ts`. The elf correctly removed the duplicate from `assess.ts` and imported from `world.ts`. Clean and correct.

### 2. Good: countInsights wiring (5a6d90e)
Previously `insightCount` was hardcoded to `0` in `setup.ts`. The elf exported the existing `countInsights` function from `world.ts` and wired it into both `buildWorldState` in `setup.ts` and the existing `readWorldState` in `world.ts`. The new test file (`count-insights.test.ts`) covers empty dir, missing dir, counting .md files, and ignoring non-.md files — thorough.

### 3. Good: Duplicate countInsights fix (b3e7004)
Correctly exported the existing function rather than adding a new one. Shows awareness of the codebase.

### 4. Minor: README tree diagram whitespace change (425ad68)
The commit realigns whitespace in the ASCII tree and adds two new conditions (`uncommitted changes` and `dead-code work-item`). The additions match the actual behaviour tree implementation — verified against the default tree. No issues.

### 5. Observation: claim-evidence.yaml additions (ced6ad9)
Added source/test entries for `archiveResolvedFindings` and `countInsights`. The entries reference actual function names and patterns. Looks correct — the evidence points to real implementations.

### 6. No issues found with test quality
The `count-insights.test.ts` tests use real filesystem operations (mkdtemp, cleanup in afterEach), test actual edge cases, and verify behaviour not just existence. No cheating patterns detected.

## Verdict

**Clean.** All changes are small, well-scoped, and tested. The RESOLVED_PATTERN dedup and countInsights wiring are genuine improvements. No bugs, no architectural violations, no spec misalignment detected.

## Status
Resolved.
