# Critique: Commits d03feaf, af00185

**Reviewer**: elf (adversarial review)
**Date**: 2026-03-22
**Scope**: Add init-templates.test.ts (62 tests) + cleanup

## Summary

New test file validates all 5 skill templates have required frontmatter (name, description, maps-to, risk) and required sections (When to apply, Instructions, Verification criteria, Permitted actions, Off-limits). Also validates config, schedule, invariants, and protocol templates. Tests grew from 339→401, all passing.

### Review

**Positives:**
- Good use of loop-generated tests for consistent validation across all skill templates
- Tests verify structural requirements from `wiki/pages/skills.md`
- Covers all template exports from init-templates.ts
- Clean, readable test structure

**Issues found:**

1. **Tests use string containment, not structural parsing** — `toContain("name:")` would match a comment containing "name:" or the word "name:" anywhere in the body. Since these are controlled template strings this is acceptable, but a frontmatter parser would be more robust. **Trivial — not blocking for template constants.**

## Status

Resolved.
