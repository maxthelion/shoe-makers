# Critique — 2026-03-25 Session (Skip review for orchestration-only commits)

## Commits reviewed

- `8480d84` Skip review for shoe-makers-only commits (findings, insights, log)

## Observations

1. **Change is minimal and well-scoped.** The single `STATE_PREFIX` string was replaced with an `ORCHESTRATION_PREFIXES` array, and the filter check updated accordingly. The function name and signature are unchanged — only the internal filtering logic broadened.

2. **Tests are thorough.** Three new test cases added: findings-only commit (filtered), insights-only commit (filtered), mixed findings+source commit (not filtered). All follow the existing test pattern using `setupGitRepo()` with signing disabled.

3. **Existing tests still pass.** The original state-file filtering tests (`candidates.md`, `work-item.md`) remain unchanged and pass because `.shoe-makers/state/` is still in the new array.

4. **Potential concern: should critiques skip review?** The change means a critique finding written by the reviewer won't trigger another review cycle. This is intentional (critiques are mechanical outputs, not code changes) but breaks the strict "every commit gets reviewed" pattern. The adversarial review is meant to catch code mistakes — findings are just text documents. Acceptable trade-off.

5. **The `ORCHESTRATION_PREFIXES` constant is exported.** This allows other modules to reuse the same prefix list if needed. Good practice.

6. **No off-limits files modified.** Only `src/verify/detect-violations.ts` and `src/__tests__/detect-violations.test.ts` — both within execute-work-item permissions.

## Verdict: Clean

## Status

Resolved.
