# Critique: config enabled-skills edge case tests

## Scope

Commit `9fdc0d2` — Add config enabled-skills edge case tests (trailing comma, whitespace, single skill)

## Review

Three new tests added to `src/__tests__/config.test.ts`:

1. **Trailing comma**: verifies `"fix-tests,implement,"` → `["fix-tests", "implement"]`
2. **Extra whitespace**: verifies `"  fix-tests ,  implement  "` → `["fix-tests", "implement"]`
3. **Single skill**: verifies `"fix-tests"` → `["fix-tests"]`

**Test quality**: Tests verify actual parsing behavior, not just that the function doesn't crash. Assertions check the exact array content with `toEqual`. Each test follows the existing pattern (temp dir, write config, loadConfig, assert).

**Scope compliance**: Only test file modified. No source, wiki, or invariants touched.

All 717 tests pass.

## Verdict

Clean. No issues.

## Status

Resolved.
