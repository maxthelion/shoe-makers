---
type: critique
date: 2026-03-22
commits: 0e10e3f, f6b20f3
status: clean
---

# Critique: Evidence pattern tightening (0e10e3f, f6b20f3)

## Commits reviewed

- `0e10e3f` — Critique: review commits f5685fa..5e58a7c — loose evidence patterns flagged
- `f6b20f3` — Tighten section-2 evidence patterns per critique-013

## Summary

The previous elf (same session) wrote a critique identifying loose evidence patterns for section-2 invariant claims, then fixed those patterns by replacing generic keywords with specific function names and code references.

## Assessment

**Clean.** The tightened patterns are materially better:

- `tickInterval` → `"tickInterval: number"` + `"intOrDefault"` — now verifies the config parsing, not just a word
- `[prioritise]` → `"function generateCandidates"` + `"assessment.openPlans"` etc. — verifies actual data flow
- `[impact, confidence, risk]` → `"function rankCandidates"` + `"weight[c.impact]"` etc. — verifies scoring formula exists
- `[work]` → `"buildInstructions"` + `"priorities.items[0]"` — verifies work reads from prioritiser output
- Reactive conditions now check `"function makeConditionAction"` + `"\"sequence\""` + `"\"condition\""` — verifies tree structure

All 301 tests pass. Invariants: 0 gaps. No off-limits files modified. Critique file is well-structured.

### Minor note

Self-review situation: the same elf wrote the critique and the fix. This is noted but acceptable since the fix is mechanical (pattern replacement) and verifiable by running invariants.

## Verdict

Clean. No issues found.

## Status

Resolved.
