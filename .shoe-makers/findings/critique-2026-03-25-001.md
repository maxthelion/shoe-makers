# Critique: Recent Doc-Sync and Test Work (2026-03-25)

## Commits Reviewed

- `8c6c479` — Doc-sync: update skills.md to reference prompt system
- `992e926` — Doc-sync: fix stale claim-evidence location reference
- `0cd132e` — Add tests for getShiftProcessPatterns async wrapper
- `76d908c` — Rename misleading shift test title per critique-211

## Overall Assessment

The work is sound. All four commits are small, focused changes that improve accuracy.

## Positive Notes

- **skills.md update** correctly replaces stale "work skill" reference with "prompt generation system" and updates the wikilink from `tick-types` to `behaviour-tree`. Verified that `src/skills/registry.ts` and `.shoe-makers/skills/` exist as referenced.
- **invariants.md update** correctly fixes the claim-evidence location from `src/verify/invariants.ts` to `.shoe-makers/claim-evidence.yaml` + `src/verify/parse-evidence.ts`. Both paths verified on disk.
- **getShiftProcessPatterns tests** are well-structured: proper temp dir setup/teardown, meaningful assertions (checks `reactiveRatio`, `reviewLoopCount`, `innovationCycleCount`), and covers the no-log, empty-log, and populated-log cases.
- **shift test rename** removes misleading comments and gives the test an honest name reflecting what it actually tests (onTick callback, not sleep path).

## Minor Advisory (no action required)

- The invariants.md commit changed `last-modified-by: user` to `last-modified-by: elf`. This is technically accurate (the elf did modify the file), but wiki pages are spec and the CLAUDE.md says "only humans maintain the spec claims". A doc-sync fixing a stale file path reference is borderline — the claim content wasn't changed, just the location reference. This seems acceptable but future elves should be cautious about wiki edits beyond mechanical fixes.

## Verdict

No issues found. Work is clean, accurate, and well-scoped.
