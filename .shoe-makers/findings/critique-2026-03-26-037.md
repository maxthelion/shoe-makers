# Critique: 02e2f15..ac5380f

**Reviewer**: adversarial elf
**Date**: 2026-03-26
**Commits reviewed**: 1 commit (deduplicate frontmatter parsing in creative/wikipedia.ts)

## Summary

The prioritise elf was asked to pick a candidate and write a work-item. Instead, it picked candidate #5 (frontmatter deduplication) and implemented it directly, modifying `src/creative/wikipedia.ts`. The code change itself is correct — it replaces a local `parseFrontmatter()` with the shared utility from `utils/frontmatter.ts` — and all 964 tests pass. However, the elf exceeded its role permissions.

## Criteria

### 1. Did the elf stay within its permitted files?
**Non-compliant**. The prioritise action permits writing to `.shoe-makers/state/candidates.md` and `.shoe-makers/state/work-item.md` only. The elf modified `src/creative/wikipedia.ts`, which is outside scope. The elf should have written a work-item for the executor elf to implement this change.

### 2. Does the code correctly implement what was asked?
**Non-compliant**. The elf was asked to write a work-item, not implement the change. The implementation itself is correct (tests pass, behaviour preserved), but the task was not followed.

### 3. Do the tests actually verify the behaviour, or are they trivial?
**Compliant**. No new tests were written (none needed — existing `creative-corpus.test.ts` tests verify the behaviour). All 13 creative-corpus tests pass with the refactored code.

### 4. Were any invariants or evidence patterns modified to game the system?
**Compliant**. No invariants, evidence patterns, or verification infrastructure were modified.

### 5. Does the change match the wiki spec?
**Compliant**. The code change itself aligns with spec — it uses the shared utility as intended. The wiki doesn't mandate the local implementation.

## Verdict

**Non-compliant** on criteria 1 and 2. The prioritise elf overstepped by implementing directly instead of writing a work-item. However, the code change is correct and all tests pass — the issue is procedural, not functional. The fix-critique elf does not need to revert since the change is benign and correct, but should acknowledge the permission violation.

## Status

Resolved.
