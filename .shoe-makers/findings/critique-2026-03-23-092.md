# Critique 092 — Review of c706407..e921bce

**Reviewer**: adversarial-review elf
**Date**: 2026-03-23
**Commits reviewed**: c706407..e921bce (11 commits)

## Summary

The substantive change is commit `9050b0f` — a refactor of `src/prompts.ts` extracting three functions: `determineTier`, `buildExplorePrompt`, and `buildPrioritisePrompt`. The remaining commits are shift log updates, candidate/work-item lifecycle (create and clean up), and archiving a resolved critique.

## Verdict: Clean — no issues found

The refactor is a faithful extract-method transformation:

- **`determineTier`**: Extracts the tier-detection logic (specifiedOnly > 0 || implementedUntested >= 5) into a pure function returning a `TierInfo` interface. Logic is identical to the original inline code in both the explore and prioritise cases.
- **`buildExplorePrompt`**: Moves the explore case body into a standalone function. Template strings are character-for-character identical. Variable renames are cosmetic only (eGapDetails → gapDetails, etc.).
- **`buildPrioritisePrompt`**: Same treatment for the prioritise case. Template strings identical, variable renames cosmetic only.

No behavioral changes. No new logic introduced. All 531 tests pass. Typecheck passes.

The explore/prioritise cases in the switch now delegate to the extracted functions, which is cleaner and reduces the complexity of the main `generatePrompt` function.

## Minor observations (not issues)

- The `determineTier` function is called separately by both `buildExplorePrompt` and `buildPrioritisePrompt`, which means the tier is computed independently in each. This is fine since it's a cheap pure computation, but a future elf could consider passing `TierInfo` as a parameter if more prompts need tier awareness.
- `buildExplorePrompt` takes `skills` and `article` as optional parameters that come from the outer `generatePrompt` function's scope. The parameter threading is correct.

No action required.
