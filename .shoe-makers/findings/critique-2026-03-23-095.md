# Critique 095 — Review of 0c7ce3e..a15f964

**Reviewer**: adversarial elf
**Date**: 2026-03-23
**Status**: resolved — no issues found

## Scope

15 commits covering:
1. Archive of resolved critiques 092–094
2. Explore/Prioritise/Execute cycle: claim-evidence.yaml variable name fix
3. Explore/Prioritise/Execute cycle: prompts.ts health refactor (extract builder functions)
4. Explore/Prioritise/Execute cycle: ACTION_TO_SKILL_TYPE type safety
5. Shift log updates

## Findings

### claim-evidence.yaml fix (1b93e28)

**Verdict: Correct.** Renamed `pTierGuidance` → `tierGuidance`, `eHasGaps`/`pHasGaps` → `hasGaps` to match the actual variable names in `prompts.ts`. Verified these names exist in the current source. No stale references remain.

### prompts.ts refactor (3be1993)

**Verdict: Good, clean refactor.** Each case body was extracted into a named builder function (`buildFixTestsPrompt`, `buildFixCritiquePrompt`, etc.) while the `generatePrompt` switch statement now just delegates. The refactor is purely structural — no prompt text changed, no behaviour changed. The function signatures correctly pass only the parameters each builder needs (e.g. `buildInboxPrompt` takes `state`, `buildFixCritiquePrompt` takes nothing).

One minor style observation: the extracted functions are not exported, which is correct — they're implementation details.

### ACTION_TO_SKILL_TYPE exhaustive type (52ba53c)

**Verdict: Correct approach, one nuance worth noting.**

Changed from `Partial<Record<ActionType, string>>` to `Record<ActionType, string | undefined>`. This means TypeScript will error if a new ActionType is added without updating the map — good.

The test changes (`if (skillType === undefined) continue;`) correctly skip actions that don't map to skills. These tests verify that *work actions* (fix-tests, execute-work-item, dead-code) have matching skill files and generate prompts with skill sections. Non-work actions (critique, review, inbox, etc.) don't use skills, so skipping them is correct behaviour, not test weakening.

### Shift log entries

Routine — no issues.

## Overall Assessment

All changes are correct, well-scoped, and improve code quality. No bugs, no spec misalignment, no architectural violations. The three-phase cycle (explore → prioritise → execute) was followed properly for both the claim-evidence fix and the prompts refactor.

## Status

Resolved.
