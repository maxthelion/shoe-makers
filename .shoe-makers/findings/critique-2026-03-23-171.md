# Critique 171 — Review of evaluate-insight (rework provenance chains)

**Reviewer**: elf (adversarial review)
**Scope**: 1 elf-authored commit — `f621dac` (rework insight) + 1 housekeeping commit

## Review

### Compliance with evaluate-insight rules

- Read insight files ✓
- Engaged constructively ✓ (found simpler version)
- Decided to **rework** (rewrote insight file with improved version) ✓
- No off-limits files modified ✓

### Rework quality

The original insight proposed YAML frontmatter provenance on state files. The reworked version simplifies to: archive consumed state files to `.shoe-makers/state/archive/` the same way findings are archived. This is a good simplification:

- Removes all the YAML frontmatter complexity ✓
- Removes commit message conventions ✓
- Requires no elf prompt changes ✓
- Builds on existing archive pattern (findings) ✓
- One-function change in setup script ✓

The reworked insight retains the core observation (decision context is lost when state files are consumed) while proposing a much simpler solution.

### Potential concerns

- The `.shoe-makers/state/` directory is gitignored. Archiving consumed files there would mean the archive is also gitignored. The insight says "git-tracked record" but if archived to `.shoe-makers/state/archive/`, it wouldn't be git-tracked. The archive directory might need to be outside `.shoe-makers/state/` — e.g., `.shoe-makers/archive/state/` — to be committed. This is a minor issue for the next evaluator to address.

## Issues Found

None blocking.

## Verdict

**Clean.** Good rework — correctly simplified the proposal from over-engineered to practical.

## Status

Resolved.
