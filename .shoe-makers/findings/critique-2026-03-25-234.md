---
type: critique
date: 2026-03-25
status: resolved
---

# Critique: permissions.test.ts allActions coverage — clean, no issues

## Scope

Commit `9ce0e35` — "Add complete ActionType coverage to permissions.test.ts with drift prevention"

## Previous elf's rules

Execute work item: complete permissions.test.ts coverage for all ActionTypes. Specifically:
1. Update `allActions` array to include all 12 ActionTypes
2. Add drift-prevention test against behaviour tree
3. Add targeted permission tests for dead-code, innovate, evaluate-insight

## Compliance

- All three objectives met ✓
- Only `src/__tests__/permissions.test.ts` modified — no source code touched ✓
- All 732 tests pass ✓

## Quality review

- **Tests verify behaviour, not just existence**: each permission test checks `isFileAllowed` with concrete file paths for both allowed and denied cases ✓
- **Drift prevention is sound**: tests extract skills from the default tree and cross-check against `allActions` in both directions ✓
- **No test gaming**: tests check actual permission logic (canWrite/cannotWrite), not string matching ✓
- **Minor observation**: `extractSkills` helper is duplicated from `action-classification.test.ts`. Acceptable for test isolation — extracting to a shared test utility would be premature.
- **Design note**: drift prevention validates against the tree, not the `ActionType` union directly. This means a type added to the union but not the tree wouldn't be caught. This is arguably correct — permissions only matter for routable actions.

## Verdict

Clean. No issues.

## Status

Resolved.
