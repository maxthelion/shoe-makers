# Candidates

## 1. Verify verification.md wiki page accuracy
**Type**: doc-sync
**Impact**: medium
**Reasoning**: `wiki/pages/verification.md` describes the review/critique system, permission roles, and cross-elf gatekeeping. After recent changes (previous-action-type snapshot, housekeeping path filtering, uncertainty tracking), the page may need updates to reflect the current verification pipeline. Files: `wiki/pages/verification.md`, `src/verify/`.

## 2. Add test for formatProcessTemperature edge cases
**Type**: test
**Impact**: low
**Reasoning**: `formatProcessTemperature()` in `src/prompts/three-phase.ts` formats reactive ratio guidance. The existing tests cover >0.6 and <0.3 thresholds but don't test exact boundary values (0.3, 0.6). Files: `src/prompts/three-phase.ts`, `src/__tests__/prompts.test.ts`.

## 3. Clean up old Blackboard types (PriorityList, CurrentTask, Verification)
**Type**: dead-code
**Impact**: medium
**Reasoning**: The old four-tick model types (`PriorityList`, `PriorityItem`, `CurrentTask`, `Verification`) are still in `types.ts` and `blackboard.ts`. After `skills/verify.ts` was removed, these types may only be used by blackboard read/write functions and their tests. If the blackboard-based workflow is fully replaced by the file-based three-phase model, these types and their blackboard functions could be dead code.

## 4. Add test for getElfChangedFiles with rename detection
**Type**: test
**Impact**: low
**Reasoning**: `getElfChangedFiles()` in `src/verify/detect-violations.ts` parses `git diff --name-only` output. Renames (which show as `old -> new` in some git formats) might not be handled. Files: `src/verify/detect-violations.ts`, `src/__tests__/detect-violations.test.ts`.

## 5. Update observability.md to mention process patterns and uncertainties
**Type**: doc-sync
**Impact**: low
**Reasoning**: `wiki/pages/observability.md` describes shift logs and findings but doesn't mention the process patterns (reactive ratio, review loop count) or uncertainties that are now surfaced in the shift log and tree trace. Files: `wiki/pages/observability.md`.
