# Candidates

## 1. Add missing `innovate` and `evaluate-insight` cases to run-skill.ts
**Type**: bug-fix
**Impact**: medium
**Reasoning**: `src/scheduler/run-skill.ts` switch statement (lines 15-47) handles all `ActionType` values except `innovate` and `evaluate-insight`. These fall through to the `default` case returning "Unknown action: ..." which gets logged as the `skillResult` in the shift log. While the shift runner (`src/scheduler/shift.ts` line 92) stops on non-explore actions so behavior is correct, the logged output is misleading. The `ActionType` union in `src/types.ts` (lines 27-38) includes both types. The test file `src/__tests__/run-skill.test.ts` also has no coverage for these cases. Fix: add two `case` statements with descriptive messages and corresponding tests.

## 2. Add tests for `src/utils/fs.ts`
**Type**: test-coverage
**Impact**: low
**Reasoning**: `src/utils/fs.ts` exports `fileExists()`, used by `src/creative/wikipedia.ts`. It's a tiny module (13 lines) with no dedicated test file. While simple, adding tests for edge cases (non-existent path, directory vs file) ensures the utility works reliably. This is the only source file in `src/utils/` without a dedicated test. Files: `src/utils/fs.ts`, new `src/__tests__/fs.test.ts`.

## 3. Verify README accuracy against current implementation
**Type**: doc-sync
**Impact**: medium
**Reasoning**: Invariant 3.5 in `.shoe-makers/invariants.md` (line 184) claims the explore step checks README accuracy. The explore prompt does include this check (line 82 of `src/prompts/three-phase.ts`), but no previous elf has actually verified whether `README.md` accurately describes the current system. The README should be checked against: actual project structure, available scripts in `package.json`, current tech stack, and implemented features. If drift is found, a doc-sync work item should be created.
