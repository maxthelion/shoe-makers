# Candidates

## 1. Add tests for prompt helper functions (determineTier, formatTopGaps, isInnovationTier)
**Type**: test
**Impact**: medium
**Reasoning**: `src/prompts/helpers.ts` contains pure functions `determineTier()`, `isInnovationTier()`, `formatTopGaps()`, `formatCodebaseSnapshot()` that control tier routing and gap presentation. These are critical to correct agent behavior but have no direct test coverage. Edge cases like empty gaps, tier boundary conditions (specOnlyCount=0, implementedUntested=5 threshold), and missing assessment data are unverified. Files: `src/prompts/helpers.ts`, needs new `src/__tests__/prompt-helpers.test.ts`.

## 2. Add tests for buildEvaluateInsightPrompt and buildInnovatePrompt
**Type**: test
**Impact**: medium
**Reasoning**: `buildEvaluateInsightPrompt()` in `src/prompts/three-phase.ts` is completely untested. `buildInnovatePrompt()` has partial coverage — missing tests for undefined article, wiki summary formatting, and the no-article fallback path. These innovation-tier prompts run unsupervised. Files: `src/prompts/three-phase.ts`, `src/__tests__/prompts.test.ts`.

## 3. Verify README.md accuracy against current implementation
**Type**: doc-sync
**Impact**: medium
**Reasoning**: README.md describes system capabilities but may be stale. Need to verify: tree action descriptions match actual implementation, config options match `load-config.ts`, project structure section matches actual `src/` layout, setup/shift instructions match actual entry points. Files: `README.md`, various `src/` files.

## 4. Process temperature signal in tree routing
**Type**: implement
**Impact**: medium
**Reasoning**: The system detects high reactive ratio (>60%) via `formatProcessTemperature()` but the signal only appears in explore prompts — doesn't affect tree routing. Adding awareness to the tree could reduce reactive churn cycles. Spec: `wiki/pages/behaviour-tree.md`. Files: `src/tree/default-tree.ts`, `src/log/shift-summary.ts`.

## 5. Add tests for shift.ts entry point lifecycle
**Type**: test
**Impact**: low
**Reasoning**: `src/shift.ts` orchestrates shift summary, log appending, and remote push. While individual components are tested, no integration test verifies the complete lifecycle. Low impact because the component tests are strong. Files: `src/shift.ts`.
