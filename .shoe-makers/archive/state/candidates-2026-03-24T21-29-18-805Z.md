# Candidates

## 1. Update observability.md to document process pattern analysis
**Type**: doc-sync
**Impact**: medium
**Reasoning**: `wiki/pages/observability.md` documents the shift log, findings, and suggestions system. It doesn't mention the process pattern analysis (`computeProcessPatterns` in `src/log/shift-log-parser.ts`) that now computes reactive ratio, review loop detection, and innovation cycle counting. These patterns are surfaced in explore/prioritise prompts as "process temperature" signals. This is a meaningful observability feature that's undocumented in the spec. Files: `wiki/pages/observability.md`, `src/log/shift-log-parser.ts`.

## 2. Add test for `getShiftProcessPatterns` with realistic multi-action log
**Type**: test-coverage
**Impact**: low
**Reasoning**: `getShiftProcessPatterns()` in `src/log/shift-log-parser.ts` is the integration point — it reads today's log and computes patterns. Current tests cover empty/minimal logs. A test with a realistic 10+ action log (mimicking an actual shift) would verify the full pipeline end-to-end. Files: `src/log/shift-log-parser.ts`, `src/__tests__/shift-log-parser.test.ts`.

## 3. Add `Review Uncommitted Work` action to parseShiftLogActions
**Type**: bug-fix
**Impact**: low
**Reasoning**: The `TITLE_TO_ACTION` mapping in `src/log/shift-log-parser.ts` has a `Review Uncommitted Work` pattern but uses the generic `/Review Uncommitted Work/i` regex. This is fine, but the "review" action it maps to might be counted differently from "critique" in reactive ratio. Verify this is intentional and the reactive ratio calculation handles both correctly. Files: `src/log/shift-log-parser.ts`, `src/log/action-constants.ts`.
