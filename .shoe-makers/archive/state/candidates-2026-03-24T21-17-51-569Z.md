# Candidates

## 1. Add known-issue entry for npm registry 403 and typecheck uncertainty
**Type**: doc-sync
**Impact**: medium
**Reasoning**: Every setup tick reports `Uncertainties: typecheckPass (missing type definitions (bun-types)), healthScore (octoclean not installed)`. This is caused by the npm registry being blocked (403) in this cloud environment, preventing `bun-types` and `octoclean` from installing. The known-issues page has entries for health scan failures but nothing about the typecheck uncertainty or how the tree handles unknowns (it currently skips the `tests-failing` condition when typecheckPass is unknown). New elves encountering this will waste time investigating. File: `.shoe-makers/known-issues.md`.

## 2. Add tests for shift-log-parser boundary conditions
**Type**: test-coverage
**Impact**: medium
**Reasoning**: `computeProcessPatterns()` in `src/log/shift-log-parser.ts` drives the process temperature signal that affects explore prompts. With 64% reactive ratio this shift, the accuracy of this calculation matters. Boundary cases to test: exactly 2 consecutive critiques (not a loop), exactly 3 (is a loop), mixed actions breaking a streak, innovation cycle counting at cap boundary. Current tests in `src/__tests__/shift-log-parser.test.ts` may not cover these edges. Files: `src/log/shift-log-parser.ts`, `src/__tests__/shift-log-parser.test.ts`.

## 3. Add test for loadSkills filtering by enabledSkills config
**Type**: test-coverage
**Impact**: low
**Reasoning**: `loadSkills()` in `src/skills/registry.ts` accepts an `enabledSkills` parameter to filter which skills are loaded. This filtering behavior should be tested — empty list, list with unknown skill names, null (all enabled). The `enabledSkills` config is documented in invariant 5.2 and now in the README. Files: `src/skills/registry.ts`, `src/__tests__/registry.test.ts`.

## 4. Verify wiki/pages/behaviour-tree.md matches current default-tree.ts
**Type**: doc-sync
**Impact**: low
**Reasoning**: `wiki/pages/architecture.md` was recently updated to match the current tree (added uncommitted-changes and dead-code nodes). The `wiki/pages/behaviour-tree.md` page may also have a stale tree diagram. If it does, it should be updated for consistency. Files: `wiki/pages/behaviour-tree.md`, `src/tree/default-tree.ts`.
