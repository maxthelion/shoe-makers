skill-type: octoclean-fix

# Move Helper Function Tests from prompts.test.ts to prompt-helpers.test.ts

## Context

`src/__tests__/prompts.test.ts` (87/100 health, 739 lines) contains duplicate helper function test blocks that also exist in `src/__tests__/prompt-helpers.test.ts` (93/100 health, 282 lines).

## What to Do

Move the following describe blocks from `prompts.test.ts` to `prompt-helpers.test.ts`:

1. **`determineTier`** (lines 552-591, 7 tests) — prompt-helpers already has 6 tests for this. The prompts.test.ts tests use `makeAssessment()` which provides additional coverage (null assessment, null invariants, boundary at 4/5 untested). Keep unique tests, skip duplicates.

2. **`isInnovationTier`** (lines 593-613, 3 tests) + **`isInnovationTier boundary`** (lines 723-739, 4 tests) — prompt-helpers already has tests. Merge unique boundary tests.

3. **`findSkillForAction`** (lines 615-649, 6 tests) — prompt-helpers already has tests. These test the same scenarios but with `makeSkillMap`/`makeSkill` factories. Keep unique tests.

4. **`formatTopGaps`** (lines 651-669, 3 tests) — prompt-helpers already has these.

5. **`formatCodebaseSnapshot`** (lines 671-694, 4 tests) — prompt-helpers already has these.

6. **`formatSkillCatalog`** (lines 696-710, 2 tests) — prompt-helpers already has tests.

### Steps

1. Read both test files carefully to identify which tests are truly unique
2. Copy unique tests from `prompts.test.ts` to `prompt-helpers.test.ts`, adapting the factory helpers as needed
3. Remove the moved describe blocks from `prompts.test.ts`
4. Remove unused imports from `prompts.test.ts` (`determineTier`, `isInnovationTier`, `findSkillForAction`, `formatTopGaps`, `formatCodebaseSnapshot`, `formatSkillCatalog`)
5. Run `bun test` to verify nothing broke

### Expected Result

- `prompts.test.ts` drops from 739 lines to ~550 lines (remove lines 552-739 = ~188 lines)
- `prompt-helpers.test.ts` gains any unique tests
- Total test count unchanged or slightly increased

## What NOT to Change

- Do NOT modify `.shoe-makers/invariants.md`
- Do NOT change any `src/` files other than the two test files
- Do NOT modify the test assertions — just move them
- Keep the `makeAssessment` and `makeStateWith` factories in `prompts.test.ts` since they're used by integration tests above

## Decision Rationale

Candidate 2 (octoclean-fix) was chosen over candidates 1 and 3 (both implement-type for structured skills) because it's a quick health improvement that resolves a known code quality issue without risk. The worst-file score of 87/100 can be significantly improved. Structured skills implementation is higher effort and can be the next work item.
