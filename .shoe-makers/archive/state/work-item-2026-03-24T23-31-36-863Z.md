# Add test coverage for architecture.branch-is-state

skill-type: test-coverage

## Context

The invariants pipeline reports `architecture.branch-is-state` as `implementedUntested`. Source evidence exists in `src/state/world.ts` (functions `getCurrentBranch` and `hasUncommittedChanges`), but no test file references these functions by name.

The claim-evidence pattern requires: `[getCurrentBranch, branchName]` in test code.

## What to build

Add tests for the branch-is-state functions in a new or existing test file.

### Tests needed

1. **`getCurrentBranch` returns the current git branch name**: Set up a test git repo, verify it returns the branch name string.
2. **`hasUncommittedChanges` returns true when files are modified**: Create a file in a test repo, stage it, verify the function returns true.
3. **`hasUncommittedChanges` returns false on a clean repo**: Verify clean repo returns false.

### Where to add tests

Check if `src/__tests__/world.test.ts` exists. If so, add there. Otherwise, create a new test file. Follow the pattern used by other tests that create temporary git repos (e.g., `src/__tests__/setup.test.ts`).

## Patterns to follow

- Use `import { getCurrentBranch, hasUncommittedChanges } from "../state/world"` — note these must be exported. If not currently exported, export them.
- Create temp dirs with `mkdtemp` and init git repos with `execSync("git init", ...)`
- Clean up temp dirs after tests

## Files to modify

- `src/__tests__/world.test.ts` (create or modify)
- `src/state/world.ts` (only if exports need adding — `getCurrentBranch` and `hasUncommittedChanges` may need `export`)

## What NOT to change

- Do not modify the claim-evidence patterns
- Do not modify `.shoe-makers/invariants.md`
- Do not modify wiki pages
- Do not modify other source files

## Decision Rationale

Candidate #1 chosen because:
- It's the only `implementedUntested` invariant — highest priority per the assessment suggestion
- The assessment explicitly says "1 implemented features need tests"
- Candidate #2 (RESOLVED_PATTERN) is a bug-fix but lower urgency since critique-073 already exposed and worked around it
- Candidate #3 (doc-sync) and #4 (git tracking) are lower impact
