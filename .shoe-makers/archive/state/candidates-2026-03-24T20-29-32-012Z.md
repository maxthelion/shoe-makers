# Candidates

## 1. Add max-ticks-per-shift and max-innovation-cycles to README config example
**Type**: doc-sync
**Impact**: low
**Reasoning**: The README config example (lines 105-111) shows 5 config keys but omits `max-ticks-per-shift` and `max-innovation-cycles`. While the README intentionally shows a brief example, these are useful for users to know about. The integration guide now has the full table, but the README should at least mention they exist. Files: `README.md`.

## 2. Add known-issue entry for npm registry 403 errors in cloud environments
**Type**: doc-sync
**Impact**: medium
**Reasoning**: Cloud environments sometimes block npm registry access (403 errors). This was encountered this session. The known-issues.md should document that `bun install` may fail but the system can still work if bun and dependencies are already cached. This helps future elves avoid wasting time on install failures. Files: `.shoe-makers/known-issues.md`.

## 3. Verify architecture.md data contracts match current Assessment type
**Type**: doc-sync
**Impact**: medium
**Reasoning**: `wiki/pages/architecture.md` describes the Assessment data contract but may not reflect fields added since the last sync (processPatterns, uncertainties, worstFiles, healthScore). The data contract section should match `src/types.ts` Assessment type. Files: `wiki/pages/architecture.md`, `src/types.ts`.

## 4. Add test for parseSimpleYaml edge cases in config loader
**Type**: test
**Impact**: low
**Reasoning**: `parseSimpleYaml()` in `src/config/load-config.ts` handles a custom YAML-like format. Edge cases worth testing: values containing colons (e.g. `key: value: with: colons`), keys with leading/trailing whitespace, completely empty files, files with only comments. Files: `src/config/load-config.ts`, `src/__tests__/config.test.ts`.
