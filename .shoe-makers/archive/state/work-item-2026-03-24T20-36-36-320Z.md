# Update architecture.md tree diagram — add missing nodes

skill-type: doc-sync

## What needs to change

`wiki/pages/architecture.md` has a tree diagram (lines 25-36) that is missing two nodes present in `src/tree/default-tree.ts`:
- `[uncommitted changes?] → Review uncommitted work` (between unreviewed commits and inbox)
- `[dead-code work-item?] → Remove dead code` (between inbox and work-item)

Current diagram:
```
├── [unreviewed commits?] → Review adversarially (direct)
├── [inbox messages?] → Handle inbox (direct)
├── [work-item.md exists?] → Execute the work item
```

Should be:
```
├── [unreviewed commits?]    → Review adversarially (direct)
├── [uncommitted changes?]   → Review uncommitted work (direct)
├── [inbox messages?]        → Handle inbox (direct)
├── [dead-code work-item?]   → Remove dead code
├── [work-item.md exists?]   → Execute the work item
```

Also add `insights/` to the "What Lives in the Repo" file tree (line 80-92) since insights are now a first-class directory used by the innovation pipeline.

## What NOT to change

- Do not modify src/, tests, or other wiki pages
- Do not modify .shoe-makers/invariants.md
- Keep existing explanatory text unchanged — only update the tree diagram and file tree

## Decision Rationale

The architecture page is a key overview page referenced by other wiki pages. Having a stale tree diagram here means any reader's first impression of the system's decision-making is inaccurate.
