# Candidates

## 1. Add tests for buildEvaluateInsightPrompt (completely untested)
**Type**: test
**Impact**: medium
**Reasoning**: `buildEvaluateInsightPrompt()` in `src/prompts/three-phase.ts` generates the prompt for the generous insight evaluator — a critical creative pipeline function with zero test coverage. The function includes insight count and disposition instructions. Files: `src/prompts/three-phase.ts`, `src/__tests__/prompts.test.ts`.

## 2. Add tests for buildInnovatePrompt edge cases
**Type**: test
**Impact**: medium
**Reasoning**: `buildInnovatePrompt()` has partial coverage but missing tests for: undefined article path (now simplified to `!!article`), wiki summary formatting, and the no-article fallback. Files: `src/prompts/three-phase.ts`, `src/__tests__/prompts.test.ts`.

## 3. Process temperature signal influencing tree routing
**Type**: implement
**Impact**: medium
**Reasoning**: The system detects high reactive ratio (>60%) but only in explore prompts. Adding tree awareness would address root cause of churn. Requires wiki update and careful design. Files: `src/tree/default-tree.ts`, `wiki/pages/behaviour-tree.md`.

## 4. Add test for state-archive module
**Type**: test
**Impact**: low
**Reasoning**: `src/archive/state-archive.ts` archives consumed state files for traceability. It's tested indirectly through setup but has no direct unit tests. Files: `src/archive/state-archive.ts`.

## 5. Wiki page for creative exploration needs update
**Type**: doc-sync
**Impact**: low
**Reasoning**: `wiki/pages/creative-exploration.md` describes the insight workflow but may not reflect recent changes (fetchArticleForAction refactor, insight frequency config). Worth verifying accuracy.
