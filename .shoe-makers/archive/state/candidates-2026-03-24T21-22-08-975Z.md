# Candidates

## 1. Add tests for shift-log-parser boundary conditions
**Type**: test-coverage
**Impact**: medium
**Reasoning**: `computeProcessPatterns()` in `src/log/shift-log-parser.ts` drives the process temperature signal affecting explore/prioritise prompts. This shift had 64% reactive ratio, showing these calculations affect behaviour. Boundary cases to test: exactly 2 consecutive critiques (below loop threshold), exactly 3 (at threshold), mixed actions breaking a streak, innovation cycle counting at the max cap. Files: `src/log/shift-log-parser.ts`, `src/__tests__/shift-log-parser.test.ts`.

## 2. Add test for loadSkills filtering by enabledSkills config
**Type**: test-coverage
**Impact**: low
**Reasoning**: `loadSkills()` in `src/skills/registry.ts` accepts `enabledSkills` to filter skills. Edge cases: empty list, unknown names, null (all enabled). Files: `src/skills/registry.ts`, `src/__tests__/registry.test.ts`.

## 3. Verify CHANGELOG.md is up-to-date with recent features
**Type**: doc-sync
**Impact**: low
**Reasoning**: Invariant 3.5 says CHANGELOG tracks user-facing changes. The [Unreleased] section may be missing recent additions like the Wikipedia creative lens, health regression detection, process pattern analysis, and the permission violation detector. Files: `CHANGELOG.md`.
