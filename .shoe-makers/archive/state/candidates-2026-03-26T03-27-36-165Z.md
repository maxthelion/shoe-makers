# Candidates

## 1. Add tests for housekeeping deterministic operations
**Type**: test
**Impact**: high
**Reasoning**: The untested invariant `spec.skills.housekeeping-tasks-archiving-shift-log-updates-should-be-ful` claims housekeeping tasks (archiving, shift log updates) are fully deterministic. The code exists in `src/setup/housekeeping.ts` (`autoCommitHousekeeping`, `isAllHousekeeping`, `HOUSEKEEPING_PATHS`) and `src/skills/assess.ts` (`archiveResolvedFindings`), but the claim-evidence YAML at `.shoe-makers/claim-evidence/25-structured-skills.yaml` lines 50-56 shows empty test evidence. This is the only untested invariant in the system — closing it brings the untested count to 0.

## 2. Improve test file health scores
**Type**: health
**Impact**: medium
**Reasoning**: Three test files are below the 90 threshold: `src/__tests__/prompts.test.ts` (87), `src/__tests__/world.test.ts` (88), `src/__tests__/setup.test.ts` (90). These are the worst files in the codebase and drag the overall health score. They likely have duplicated test setup, overly long test cases, or could benefit from extracting shared helpers. Improving them from 87-90 to 93+ would improve overall codebase health.

## 3. Doc-sync wiki for new note-type findings feature
**Type**: doc-sync
**Impact**: medium
**Reasoning**: The note-type findings feature was just implemented (auto-archive notes >24h, inject notes into prompts via `readNotes()` in `src/setup/format-action.ts`). The wiki doesn't document this yet. The relevant pages to update would be `wiki/pages/verification.md` (findings section) or `wiki/pages/architecture.md` (state files section) to mention that `note-*` files in `.shoe-makers/findings/` are ephemeral elf-to-elf context that auto-archive after 24 hours.

## 4. Stale invariants require human attention (not elf-actionable)
**Type**: doc-sync
**Impact**: high
**Reasoning**: Open finding `.shoe-makers/findings/stale-invariants-skills-list.md` identifies three issues: section 3.2 lists only 5 of 9 implemented skills as current, section 2.2 is missing the "uncommitted changes → review" reactive condition and the dead-code node, and sections 1.6 and 3.4 have duplicate numbering. However, this requires modifying `.shoe-makers/invariants.md` which is off-limits to elves — flagging for human action. The finding is already open and visible.
