# Candidates

## 1. Consolidate test helpers to reduce duplication in worst health files
**Type**: health
**Impact**: high
**Reasoning**: The three worst health files (`src/__tests__/prompts.test.ts` at 87, `src/__tests__/world.test.ts` at 88, `src/__tests__/setup.test.ts` at 90) all define their own `makeAssessment()` and `makeWorldState()` helpers with incompatible signatures. `src/__tests__/test-utils.ts` already exports shared helpers but the test files don't use them consistently. Consolidating these into `test-utils.ts` would reduce ~80 lines of duplicated code across the three files and improve all three health scores. This is the highest-leverage change for the health metric.

## 2. Update README.md example config to list all 9 skills
**Type**: doc-sync
**Impact**: medium
**Reasoning**: `README.md` line 115 shows `enabled-skills: fix-tests, implement, test-coverage, doc-sync, health` but the system now has 9 skills (adding `bug-fix`, `dead-code`, `dependency-update`, `octoclean-fix`). The "omit to enable all" instruction is correct but the example is misleading about what's available. Small fix with immediate clarity benefit for human users.

## 3. Stale invariants.md requires human action (not elf-actionable)
**Type**: doc-sync
**Impact**: high
**Reasoning**: Open finding `.shoe-makers/findings/stale-invariants-skills-list.md` documents: section 3.2 lists only 5 of 9 skills as current, section 2.2 is missing the "uncommitted changes → review" and "dead-code work-item" reactive conditions, and sections 1.6 and 3.4 have duplicate numbering. This requires modifying `.shoe-makers/invariants.md` which is off-limits to elves. The finding is already open and visible — flagging again for human attention.
