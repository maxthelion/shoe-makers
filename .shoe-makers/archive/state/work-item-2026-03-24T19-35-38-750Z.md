# Work Item: Sync behaviour-tree.md tree diagram with actual default-tree.ts

skill-type: doc-sync

## Context

The tree diagram in `wiki/pages/behaviour-tree.md` (lines 20-29) is missing two nodes that exist in `src/tree/default-tree.ts`:
1. `[uncommitted changes?] → Review uncommitted work` (between `unreviewed commits` and `inbox messages`)
2. `[dead-code work-item?] → Remove dead code` (between `inbox messages` and `work-item.md exists`)

The actual tree order in default-tree.ts is:
1. tests-failing → fix-tests
2. unresolved-critiques → fix-critique
3. unreviewed-commits → critique
4. unverified-work → review
5. inbox-messages → inbox
6. dead-code-work → dead-code
7. work-item → execute-work-item
8. candidates → prioritise
9. insights → evaluate-insight
10. innovation-tier → innovate
11. explore → explore

## What to do

Update the tree diagram in `wiki/pages/behaviour-tree.md` to include all 11 nodes matching the actual tree order. Keep the existing format with `├──` and `└──`.

## What NOT to change

- Do not modify other wiki pages
- Do not modify code
- Keep the existing annotations (direct prompt, etc.)

## Decision Rationale

The wiki is the spec — an incomplete tree diagram could mislead future elves or humans about what conditions the tree checks. This is the same kind of doc-sync done for README.md earlier this shift.
