# Candidates

## 1. [Implement structured prioritise skill — pre-fill work-item output template]
**Type**: implement
**Impact**: high
**Reasoning**: Per `wiki/pages/structured-skills.md` lines 79-82, the `write-work-item` (prioritise) skill should enforce required sections (title, skill-type, wiki text, instructions, tests, off-limits, decision rationale). Currently `src/prompts/prioritise.ts` includes candidates content but the work-item output format is free-form. Following the structured critique and explore patterns, pre-fill the work-item template sections so the elf only provides the intelligent parts. The prioritise action runs every cycle after explore, making format compliance improvements compound.

## 2. [Implement structured innovate skill — enforce insight output format with article reference]
**Type**: implement
**Impact**: medium
**Reasoning**: Per `wiki/pages/structured-skills.md` lines 84-87, the `write-insight` (innovate) skill should enforce Lens section with article title, Connection, Proposal, and Why sections. Currently `src/prompts/innovate.ts` includes the article but doesn't enforce output structure with pre-filled placeholders. The innovate prompt is the most creative skill — format compliance failures waste ticks when insights get rejected for missing sections.

## 3. [Implement structured evaluate-insight skill — enforce evaluation output format]
**Type**: implement
**Impact**: medium
**Reasoning**: Per `wiki/pages/structured-skills.md` lines 89-92, the `evaluate-insight` skill should enforce evaluation, build-on-it, and decision sections with specific decision options (promote/rework/dismiss). Currently `src/prompts/evaluate-insight.ts` describes the options but doesn't pre-fill a template. This is the last of the three-phase skills to structure.
