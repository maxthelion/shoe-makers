# Candidates

## 1. Add test for state-archive traceability
**Type**: test
**Impact**: medium
**Reasoning**: `src/archive/state-archive.ts` archives consumed state files (candidates.md, work-item.md) to `.shoe-makers/archive/state/` with timestamps for traceability. The archive test (`src/__tests__/archive.test.ts`) exists but should verify the timestamp format and skill-based file selection. Files: `src/archive/state-archive.ts`, `src/__tests__/archive.test.ts`.

## 2. Verify creative-exploration.md wiki page accuracy
**Type**: doc-sync
**Impact**: medium
**Reasoning**: `wiki/pages/creative-exploration.md` describes the insight workflow. Recent changes include: fetchArticleForAction refactor, insight frequency config, innovation cycle caps, and the evaluate-insight generous disposition. The wiki should reflect these. Files: `wiki/pages/creative-exploration.md`, `src/creative/wikipedia.ts`, `src/tree/default-tree.ts`.

## 3. Add test for schedule.ts midnight-wrap edge cases
**Type**: test
**Impact**: low
**Reasoning**: `src/schedule.ts` handles overnight shifts that cross midnight (start: 22, end: 6). The schedule tests exist but edge cases around midnight wrap and shift date calculation would improve confidence. Files: `src/schedule.ts`, `src/__tests__/schedule.test.ts`.

## 4. Consolidate types.ts — Assessment type has grown organically
**Type**: health
**Impact**: low
**Reasoning**: The `Assessment` type in `src/types.ts` has accumulated fields over time (uncertainties, processPatterns, etc). A review to ensure it's clean and well-documented would improve maintainability. Files: `src/types.ts`.

## 5. Add test for shift-log appendToShiftLog concurrency safety
**Type**: test
**Impact**: low
**Reasoning**: `appendToShiftLog()` in `src/log/shift-log.ts` appends to a shared file. Multiple setup runs could theoretically interleave. Testing the append behavior under concurrent access would verify correctness. Files: `src/log/shift-log.ts`, `src/__tests__/shift-log.test.ts`.
