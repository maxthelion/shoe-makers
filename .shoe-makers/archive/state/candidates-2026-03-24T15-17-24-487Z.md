# Candidates

## 1. Add claim-evidence entries and tests for Section 2.6.1 innovate observability
**Type**: implement
**Impact**: high
**Reasoning**: All 5 specified-only invariants are from section 2.6.1. The code partially exists (setup.ts logs article title, prompt enforces lens) but there are no claim-evidence entries in `.shoe-makers/claim-evidence.yaml` and no tests verifying these behaviours. Adding evidence rules + tests would clear the 5 specified-only gap and raise health score. Files: `.shoe-makers/claim-evidence.yaml`, `src/__tests__/prompts.test.ts`, `src/__tests__/setup.test.ts`.

## 2. Implement insight filename logging in shift log after innovate ticks
**Type**: implement
**Impact**: high
**Reasoning**: Invariant 2.6.1 bullet 3 requires "the shift log entry for an innovate tick should include: the Wikipedia article title, whether an insight was written, and the insight filename." Currently setup.ts logs the article title at fetch time, but after the elf runs, there's no code to detect which insight file was created and log it. This needs to happen in setup.ts on the *next* tick (check for new insight files since last tick). Files: `src/setup.ts`, `src/state/world.ts`.

## 3. Improve prompts.test.ts health score
**Type**: health
**Impact**: medium
**Reasoning**: `src/__tests__/prompts.test.ts` is the worst-scoring file (87). The explore agent found incomplete creative lens test coverage — tests check that `generatePrompt()` includes a lens section but don't verify the Wikipedia article title flows through to the innovate prompt specifically. Adding targeted tests for innovate prompt content would improve both coverage and health. File: `src/__tests__/prompts.test.ts`.

## 4. Add insight lifecycle tracking to shift summary
**Type**: implement
**Impact**: medium
**Reasoning**: `formatShiftSummary()` and `formatDashboard()` in `src/log/shift-log.ts` don't mention insights. The shift summary should include how many insights were written and evaluated during the shift, so the human's morning review shows creative pipeline activity at a glance. Files: `src/log/shift-log.ts`, `src/log/shift-summary.ts`, `src/__tests__/shift-summary.test.ts`.

## 5. Test insight file validation — verify article reference exists
**Type**: test
**Impact**: low
**Reasoning**: Invariant 2.6 says "The insight file **must** reference the Wikipedia article." The prompt instructs the elf to do this, but there's no validation or test that checks an insight file actually contains the article reference. A test could verify that the prompt output format is parseable and contains the expected article title. This is defensive — the prompt change should handle it, but a test confirms it. File: new test or extend `src/__tests__/prompts.test.ts`.
