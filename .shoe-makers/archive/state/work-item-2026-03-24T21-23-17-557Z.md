# Update CHANGELOG with recent features

skill-type: doc-sync

## What needs to change

`CHANGELOG.md` [Unreleased] section (starts at line 8) is missing several features that have been added since its last update. Add entries for:

### Added
- Wikipedia creative lens for explore and innovate actions — random article as analogical thinking prompt
- Health regression detection — warns when health score drops between ticks
- Process pattern analysis — shift log parsing detects reactive ratio and review loops
- Process temperature signals in explore/prioritise prompts — high reactive ratio triggers root-cause guidance
- Permission violation detection — setup detects when elves modify files outside their permitted scope
- Innovation cycle cap — `max-innovation-cycles` config prevents diminishing-returns loops
- State file archiving — consumed candidates and work items archived for traceability
- `insight-frequency` config — controls probability of creative lens in explore ticks
- Known issues documentation for cloud environment quirks

Check the existing entries to avoid duplicating anything already listed. Add only genuinely missing features.

## What NOT to change

- Do not modify src/, tests, or wiki pages
- Do not modify .shoe-makers/invariants.md
- Keep the existing CHANGELOG entries unchanged — only add new entries to [Unreleased]
- Follow the Keep a Changelog format already used in the file

## Decision Rationale

Picked over shift-log-parser tests because those tests already have good boundary coverage (checked — 12 tests cover all key edges). CHANGELOG staleness is a real gap per invariant 3.5 and the file hasn't been updated in several shifts despite significant feature additions.
