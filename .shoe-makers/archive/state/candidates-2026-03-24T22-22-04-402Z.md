# Candidates

## 1. Add tests for `src/utils/fs.ts`
**Type**: test-coverage
**Impact**: low
**Reasoning**: `src/utils/fs.ts` exports `fileExists()` used by `src/creative/wikipedia.ts`. It's a 13-line module — the only file in `src/utils/` without a dedicated test. Edge cases to cover: non-existent path returns false, existing file returns true, directory returns true. Files: `src/utils/fs.ts`, new `src/__tests__/fs-utils.test.ts`.

## 2. Verify and document README accuracy
**Type**: doc-sync
**Impact**: medium
**Reasoning**: Invariant 3.5 claims the explore step checks README accuracy. The README at 149 lines is comprehensive and appears mostly accurate — the tree diagram matches `src/tree/default-tree.ts`, config options match `src/config/load-config.ts`. However, a systematic check has not been done. Specifically: verify that `bun run tick` (README line 90) still works as documented, check that all listed config keys in the YAML example (lines 106-114) are actually parsed, and confirm the project structure section (lines 124-140) matches the actual directory layout. If drift is found, fix it.

## 3. Add `dead-code` test to `src/__tests__/run-skill.test.ts`
**Type**: test-coverage
**Impact**: low
**Reasoning**: While the `dead-code` case exists in `run-skill.ts` (lines 39-40), there's no corresponding test for it in `src/__tests__/run-skill.test.ts`. The test file covers fix-tests, fix-critique, critique, review, inbox, execute-work-item, prioritise, innovate, evaluate-insight, and unknown — but `dead-code` is missing. A simple test following the existing pattern would close this gap. Files: `src/__tests__/run-skill.test.ts`.
