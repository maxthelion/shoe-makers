skill-type: doc-sync

# Update Verification Wiki — Document review-loop-with-candidates Node

## Wiki Spec

From `wiki/pages/wiki-as-spec.md`: The wiki is the source of truth. Code is derived from the spec. When code diverges from wiki, the wiki should be updated.

Advisory finding from critique-2026-03-26-002: The code has a `review-loop-with-candidates` node that isn't in the wiki.

## Current Code

`src/tree/default-tree.ts:109`:
```ts
makeConditionAction("review-loop-with-candidates", (s) => inReviewLoop(s) && s.hasCandidates, "prioritise"),
```

This node sits BEFORE the existing `review-loop-breaker` (line 110) and routes to `prioritise` when in a review loop AND candidates exist. This prevents wasted ticks re-exploring when exploration is already done.

## Current Wiki

`wiki/pages/verification.md:124-138` — Tree order diagram shows:
```
├── [review loop ≥3?]        → Break out to explore
```

But the actual tree has:
```
├── [review loop ≥3 + candidates?] → Prioritise (consume existing candidates)
├── [review loop ≥3?]              → Break out to explore
```

## What to Change

In `wiki/pages/verification.md`:

1. Update the tree diagram (lines 124-138) to include the `review-loop-with-candidates` node:
```
├── [review loop ≥3 + candidates?] → Prioritise (consume candidates)
├── [review loop ≥3?]              → Break out to explore
```

2. Update the review-loop explanation (line 140) to mention both nodes:
   - The original: "If the shift has seen 3+ review loop iterations, the tree routes to explore"
   - Add: "If candidates already exist, the tree routes to prioritise instead to consume them, avoiding a redundant explore tick."

## Also Update

`wiki/pages/behaviour-tree.md` — Check if it has a tree diagram that needs the same update.

## What NOT to Change

- Do NOT modify `.shoe-makers/invariants.md`
- Do NOT modify any `src/` files — this is doc-sync only
- Do NOT change the tree routing code — only update documentation

## Decision Rationale

Candidate 2 (doc-sync) was chosen because it addresses an outstanding advisory finding from a previous critique. The wiki-as-spec principle requires keeping documentation current with code changes. This is a quick win that resolves a known discrepancy. Candidate 1 (structured explore) is higher effort and can be the next work item.
