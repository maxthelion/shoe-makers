# Candidates

## 1. Add `Review Uncommitted Work` to invariant 2.2 reactive conditions (finding)
**Type**: doc-sync
**Impact**: medium
**Reasoning**: Invariant 2.2 lists 4 reactive conditions: tests failing, unresolved critiques, unreviewed commits, inbox messages. But the actual tree has 5 reactive conditions — `uncommitted changes` is missing from the invariant. Additionally, invariant 2.2 doesn't mention `dead-code work-item` which sits between inbox and the three-phase zone. This is a human-maintained file, so write a finding suggesting the update. Files: `.shoe-makers/invariants.md` (human-only), existing finding `stale-invariants-skills-list.md`.

## 2. Improve the existing stale-invariants finding with specific section references
**Type**: doc-sync
**Impact**: low
**Reasoning**: The existing `stale-invariants-skills-list.md` finding mentions section 3.2 skills list and duplicate section numbering (1.6, 3.4). It could be expanded to also note the missing reactive conditions in section 2.2 and the missing dead-code node. This consolidates all known invariant staleness into one finding for the human. Files: `.shoe-makers/findings/stale-invariants-skills-list.md`.

## 3. Add test for `parseActionTypeFromPrompt` with all action types
**Type**: test-coverage
**Impact**: low
**Reasoning**: `parseActionTypeFromPrompt()` in `src/prompts/helpers.ts` maps prompt title text back to ActionType. While `src/__tests__/prompt-helpers.test.ts` likely tests this, verifying that all 11 tree actions round-trip correctly through generatePrompt → parseActionTypeFromPrompt would be a useful integration check. Files: `src/prompts/helpers.ts`, `src/__tests__/prompt-helpers.test.ts`.
