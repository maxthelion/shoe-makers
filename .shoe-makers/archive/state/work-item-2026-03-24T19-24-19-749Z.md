# Work Item: Sync README.md tree diagram with actual default-tree.ts

skill-type: doc-sync

## Context

The README.md tree diagram (lines 11-22) is missing two nodes that exist in `src/tree/default-tree.ts`:
- `[insights exist?] → Evaluate insight` (line 105 of default-tree.ts)
- `[innovation tier?] → Innovate` (line 106 of default-tree.ts)

The README shows `[always true] → Explore` as the last node but doesn't show the innovation-tier or insights nodes above it.

## What to do

1. Update the tree diagram in README.md (lines 11-22) to match the actual tree in `src/tree/default-tree.ts`
2. Add brief descriptions of the innovation tier and insight evaluation in the three-phase orchestration section
3. Do NOT change anything else in the README — it's otherwise accurate
4. Run `bun test` to confirm nothing breaks (tests don't depend on README, but verify anyway)

## Exact tree diagram to use

```
Selector
├── [tests failing?]         → Fix tests
├── [unresolved critiques?]  → Fix issues flagged by reviewer
├── [unreviewed commits?]    → Adversarial review of previous elf's work
├── [uncommitted changes?]   → Review uncommitted work
├── [inbox messages?]        → Handle human instructions
├── [dead-code work-item?]   → Remove dead code
├── [work-item.md exists?]   → Execute the detailed work item
├── [candidates.md exists?]  → Prioritise: pick one, write work-item.md
├── [insights exist?]        → Evaluate insight (generous/convergent)
├── [innovation tier?]       → Innovate: creative brief with Wikipedia lens
└── [always true]            → Explore: assess the codebase, write candidates.md
```

## Decision Rationale

Chosen because the README is the project's public face and an inaccurate tree diagram misleads new users about the system's capabilities. The innovation tier and insight evaluation are core features (covered in wiki/pages/creative-exploration.md and wiki/pages/behaviour-tree.md) that deserve visibility. Quick, low-risk, high-clarity improvement.
