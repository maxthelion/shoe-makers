# Candidates

## 1. Add max-ticks-per-shift and max-innovation-cycles to integration.md config table
**Type**: doc-sync
**Impact**: medium
**Reasoning**: `wiki/pages/integration.md` has a config table (lines 89-96) listing available config keys, but it's missing `max-ticks-per-shift` (default 10) and `max-innovation-cycles` (default 3) which are both valid config keys in `src/config/load-config.ts`. Users reading the integration guide won't know these are configurable. Files: `wiki/pages/integration.md`, `src/config/load-config.ts`.

## 2. Add test for archiveConsumedStateFiles missing directory handling
**Type**: test
**Impact**: low
**Reasoning**: `src/archive/state-archive.ts` creates archive directories if missing, but this path may not be tested. Edge case: what happens when archive directory doesn't exist yet. Files: `src/archive/state-archive.ts`, `src/__tests__/state-archive.test.ts`.

## 3. Document the assessment uncertainties field in architecture.md
**Type**: doc-sync
**Impact**: low
**Reasoning**: `wiki/pages/architecture.md` describes the assessment cache but doesn't mention the `uncertainties` field or how the tree handles unknown values. The observability.md page now documents uncertainties, but architecture.md's data contracts section should also reference it since it's part of the Assessment type. Files: `wiki/pages/architecture.md`, `src/types.ts`.

## 4. Add known-issue entry for npm registry 403 errors in cloud environments
**Type**: doc-sync
**Impact**: medium
**Reasoning**: The npm registry is sometimes blocked in cloud environments (403 errors on all packages). This causes `bun install` to fail, but bun may already be installed. The known-issues.md should document that if bun is already installed and node_modules exists, the elf can skip `bun install` and proceed. This was encountered in this session. Files: `.shoe-makers/known-issues.md`.
