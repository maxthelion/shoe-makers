# Candidates

## 1. Update observability.md to document process patterns and uncertainties
**Type**: doc-sync
**Impact**: medium
**Reasoning**: `wiki/pages/observability.md` describes shift logs, findings, and suggestions but doesn't mention process patterns (reactive ratio, review loop count, innovation cycle count) or the uncertainty tracking system that now appears in tree traces and shift log entries. These were added to the codebase (`src/log/shift-log-parser.ts`, `src/skills/assess.ts`) but the wiki page was never updated. The explore prompt itself surfaces reactive ratio guidance (e.g. "high reactive ratio (63%)"), showing this is a key observability feature. Files: `wiki/pages/observability.md`, `src/log/shift-log-parser.ts`, `src/skills/assess.ts`.

## 2. Stale skills list in invariants.md — finding for human
**Type**: doc-sync
**Impact**: medium
**Reasoning**: Invariants section 3.2 says "Current skills: fix-tests, implement, test-coverage, doc-sync, health" and "Planned: octoclean-fix, bug-fix, dependency-update, dead-code removal." But all 9 skills now exist in `.shoe-makers/skills/`. The invariants are stale — the planned skills are implemented. Since elves cannot modify invariants.md, this should be written as a finding for the human to update. Also, sections 1.6 and 3.4 each appear twice (duplicate numbering). Files: `.shoe-makers/invariants.md`.

## 3. Add integration test for setup.ts branch management
**Type**: test
**Impact**: medium
**Reasoning**: `src/setup.ts` has `ensureBranch()` and `checkoutOrCreateBranch()` functions that handle branch creation and checkout with git operations. The existing setup tests (`src/__tests__/setup.test.ts`) test formatting functions but the branch management logic is untested. Since branch management is a core part of the shift lifecycle and errors there could cause the system to work on the wrong branch, this is worth testing. Files: `src/setup.ts`, `src/__tests__/setup.test.ts`.

## 4. Add test for schedule.ts getShiftDate with edge cases
**Type**: test
**Impact**: low
**Reasoning**: `src/schedule.ts` has `getShiftDate()` which handles midnight-wrapping shifts (e.g. start: 22, end: 6 means work at 1am should use yesterday's date). The schedule test file covers `isWithinWorkingHours` but `getShiftDate` edge cases around midnight boundary (exactly at end hour, exactly at start hour) may not be fully covered. Files: `src/schedule.ts`, `src/__tests__/schedule.test.ts`.

## 5. Root cause analysis: high reactive ratio from review/critique loop overhead
**Type**: doc-sync
**Impact**: low
**Reasoning**: The process signal shows 63% reactive ratio. Looking at the shift log pattern, the system spends significant ticks in review/critique loops for every piece of work. This is by design (cross-elf gatekeeping) but means ~2-3 ticks of overhead per productive tick. The observability wiki page could document expected ratios and what "healthy" looks like, so future humans reviewing shifts know whether 63% reactive is normal or indicates a problem. Files: `wiki/pages/observability.md`.
