# Candidates

## 1. Implement structured explore skill — pre-fill codebase metrics and output template
**Type**: implement
**Impact**: high
**Reasoning**: Per `wiki/pages/structured-skills.md` lines 74-77, the `write-candidates` (explore) skill should have setup provide invariant counts, health scores, findings, and tier classification pre-filled, with the template enforcing minimum 3 candidates and required fields (type, impact, reasoning). Currently `src/prompts/explore.ts` passes this data as prose guidance. Following the structured critique pattern (`src/setup/critique-context.ts`), create an `ExploreContext` that pre-computes all metrics and builds a structured output template with `[YOUR JUDGEMENT HERE]` placeholders for the actual candidate reasoning.

## 2. Reduce prompts.test.ts — move helper function tests to prompt-helpers.test.ts
**Type**: octoclean-fix
**Impact**: medium
**Reasoning**: `src/__tests__/prompts.test.ts` (87/100 health, 739 lines) tests both `generatePrompt` integration and helper functions (`determineTier`, `isInnovationTier`, `findSkillForAction`, formatters). The helper tests (~200 lines) belong in `src/__tests__/prompt-helpers.test.ts` (93/100 health, 363 lines) where related helper tests already live. Moving them would bring prompts.test.ts down to ~540 lines and improve its health score. This was identified in two previous explore cycles.

## 3. Implement structured prioritise skill — pre-fill candidates and output template
**Type**: implement
**Impact**: medium
**Reasoning**: Per `wiki/pages/structured-skills.md` lines 79-82, the `write-work-item` (prioritise) skill should have setup provide candidates.md content and relevant wiki/code context, with the template enforcing required sections (title, skill-type, instructions, tests, off-limits, decision rationale). Currently `src/prompts/prioritise.ts` passes candidates but doesn't use structured output templates. This pairs naturally with the structured explore skill.
