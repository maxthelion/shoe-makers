skill-type: implement

# Implement Structured Explore Skill — Pre-fill Output Template

## Wiki Spec

From `wiki/pages/structured-skills.md` lines 74-77:
> **write-candidates** — explore phase output
> - Setup provides: invariant counts, health scores, findings, tier classification
> - Template defines: minimum 3 candidates, required fields (type, impact, reasoning), file path
> - Elf provides: the actual candidates with reasoning

## Current Code

`src/prompts/explore.ts:35-106` — `buildExplorePrompt()` already includes tier information, gap details, creative lens, skill catalog, and an example output format. But the output section is a loose example, not a pre-filled template.

## What to Build

1. **Update `buildExplorePrompt()` in `src/prompts/explore.ts`** to include a structured output template with pre-filled candidate slots:

Replace the loose output example (lines 86-99) with a pre-filled template like:
```markdown
## Output

Write `.shoe-makers/state/candidates.md` with exactly this format:

# Candidates

## 1. [YOUR TITLE HERE]
**Type**: [implement | test-coverage | fix | health | doc-sync | octoclean-fix | dead-code | bug-fix | dependency-update]
**Impact**: [high | medium | low]
**Reasoning**: [YOUR REASONING HERE — reference specific file paths, wiki pages, and invariant gaps]

## 2. [YOUR TITLE HERE]
**Type**: [choose from types above]
**Impact**: [high | medium | low]
**Reasoning**: [YOUR REASONING HERE]

## 3. [YOUR TITLE HERE]
**Type**: [choose from types above]
**Impact**: [high | medium | low]
**Reasoning**: [YOUR REASONING HERE]

You may add candidates 4 and 5 if you find additional high-value items.
```

The key changes:
- Include valid skill types from the loaded skills (dynamically from the `skills` parameter)
- Pre-fill 3 numbered candidate slots with placeholder fields
- Make the `Type` field show only valid types from the skill registry
- Keep the instructions about specificity and minimum 3 candidates

2. **Update the skill types in the template** to be dynamically generated from the available skills map, so the elf sees only valid types.

## Patterns to Follow

- The structured critique prompt (`src/prompts/critique.ts`) uses `[YOUR JUDGEMENT HERE]` placeholders — use the same pattern
- Keep the existing `tierSection`, `processSection`, `lensSection` logic unchanged
- Only modify the output section and skill type list

## Tests to Write

In `src/__tests__/prompts.test.ts` or `src/__tests__/explore-prompt.test.ts`:

1. Test that `buildExplorePrompt` output includes pre-filled candidate slots (## 1., ## 2., ## 3.)
2. Test that output includes valid Type options
3. Test that `[YOUR` placeholder markers appear in output
4. Test that existing features (tier section, creative lens, process temperature) still work

## What NOT to Change

- Do NOT modify `.shoe-makers/invariants.md`
- Do NOT modify wiki pages
- Do NOT change tier detection logic, creative lens, or process temperature
- Do NOT change the steps section (1-7) — only the output section
- Keep backward compatibility — existing tests should still pass

## Decision Rationale

Candidate 1 (structured explore) was chosen because it follows naturally from the structured critique implementation and addresses the most frequently-used structured skill (explore runs every cycle when nothing is queued). Candidates 2 and 3 (prioritise and innovate) can follow in subsequent ticks.
