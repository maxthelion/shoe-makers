# Work Item: Add "Patterns and Gotchas" section to known-issues.md

## Origin

Insight 2026-03-24-002 (mycorrhizal network lens) — expand known-issues.md beyond troubleshooting to include non-bug knowledge that reduces reactive churn.

## Improved version

Add a "## Patterns and Gotchas" section to `.shoe-makers/known-issues.md` documenting common gotchas that aren't bugs but cause wasted cycles. Include:

1. **Evidence pattern matching**: Markdown bold `**text**` in template literals needs escaping in regex patterns. Claim IDs are auto-generated by slugifying page titles to 60 chars — always verify exact IDs by reading `invariants.md`.
2. **Frontmatter field names**: `getFrontmatterField` now escapes regex metacharacters, but callers should still use simple field names.
3. **Invariant numbering**: The `###` regex for extracting claims matches two-level numbering (2.6), not three-level (2.6.1) — must check the actual claim extraction code.

## Files to modify

- `.shoe-makers/known-issues.md` — add new section

## Tests

No tests needed — this is documentation.

## Skill type

execute-work-item

## Patterns to follow

Follow the existing format in known-issues.md: symptom/cause/fix structure for troubleshooting, and a simpler description/example structure for the new gotchas section.
