# Candidates

## 1. Add missing `dead-code` test case to run-skill.test.ts
**Type**: test-coverage
**Impact**: low
**Reasoning**: `src/__tests__/run-skill.test.ts` covers all `ActionType` values except `dead-code`. The `dead-code` case exists in `src/scheduler/run-skill.ts` (lines 39-40) but has no corresponding test. This is a gap left from the previous test coverage expansion that added `innovate` and `evaluate-insight` tests. Simple fix: add one test following the existing pattern. Files: `src/__tests__/run-skill.test.ts`.

## 2. Add tests for `src/utils/fs.ts` fileExists utility
**Type**: test-coverage
**Impact**: low
**Reasoning**: `src/utils/fs.ts` exports `fileExists()`, a 13-line utility used by `src/creative/wikipedia.ts`. It's the only source file in `src/utils/` without a dedicated test. Edge cases: non-existent path, existing file, existing directory. Files: `src/utils/fs.ts`, new `src/__tests__/fs-utils.test.ts`.

## 3. Sync verification wiki page role names with current ActionType values
**Type**: doc-sync
**Impact**: medium
**Reasoning**: `wiki/pages/verification.md` permission table lists roles like "plan-implementer", "implementer", "test-writer", "doc-writer", "refactorer", "assessor" that don't directly correspond to `ActionType` values in `src/types.ts`. The table uses condition names like "Open plans?", "Specified-only invariant?", "Untested code?", "Undocumented code?", "Code health?" — but the actual tree in `src/tree/default-tree.ts` doesn't have these conditions. These conditions route through the three-phase cycle (explore → prioritise → execute) with skill types rather than direct tree conditions. The wiki page should clarify that these roles apply to work items with specific `skill-type` values, not as separate tree conditions. Files: `wiki/pages/verification.md`, `src/tree/default-tree.ts`, `src/verify/permissions.ts`.
