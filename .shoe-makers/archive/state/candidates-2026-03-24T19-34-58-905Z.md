# Candidates

## 1. Clean up old types in types.ts that may be partially dead
**Type**: health
**Impact**: medium
**Reasoning**: `types.ts` contains `Verification`, `CurrentTask`, `PriorityList`, `PriorityItem` — types from the old four-tick model. While they're still referenced by blackboard.ts and its tests, they may represent dead functionality if the blackboard-based workflow (assess→prioritise→work→verify) is no longer the active execution model. Need to trace whether these types are used in any production code path or only in tests/blackboard reads. Files: `src/types.ts`, `src/state/blackboard.ts`.

## 2. Add integration test for behaviour tree + world state end-to-end
**Type**: test
**Impact**: medium
**Reasoning**: `src/__tests__/default-tree.test.ts` tests the tree with mocked world state, but no test verifies the full pipeline: assess → build world state → evaluate tree → format action. This would catch regressions in the assessment → tree → prompt chain. Files: `src/setup.ts`, `src/__tests__/setup.test.ts`.

## 3. Verify behaviour-tree.md matches actual tree definition
**Type**: doc-sync
**Impact**: medium
**Reasoning**: `wiki/pages/behaviour-tree.md` describes the tree structure. After changes like adding innovation tier, insight evaluation, and dead-code work items, the wiki tree diagram may be stale. Should verify it matches `src/tree/default-tree.ts`. Files: `wiki/pages/behaviour-tree.md`, `src/tree/default-tree.ts`.

## 4. Add test for schedule.ts getShiftDate midnight-wrap
**Type**: test
**Impact**: low
**Reasoning**: `getShiftDate()` in `src/schedule.ts` handles overnight shifts crossing midnight. While tested for basic cases, edge cases around DST boundaries and exact midnight times would improve robustness. Files: `src/schedule.ts`, `src/__tests__/schedule.test.ts`.

## 5. Document the three-phase cycle in behaviour-tree.md
**Type**: doc-sync
**Impact**: low
**Reasoning**: The three-phase cycle (explore → prioritise → execute) is a core concept but `wiki/pages/behaviour-tree.md` may not fully describe how the phases interact through candidates.md and work-item.md state files. Files: `wiki/pages/behaviour-tree.md`.
