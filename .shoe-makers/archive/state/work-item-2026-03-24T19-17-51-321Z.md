# Work Item: Remove dead code module verify.ts

skill-type: dead-code

## Context

`src/skills/verify.ts` is a leftover from the old four-tick model. It was identified as dead code in a previous audit (`findings/archive/spec-code-audit-2026-03-22.md`). The other dead modules (`prioritise.ts`, `work.ts`) were already removed in prior sessions.

## What to do

1. Delete `src/skills/verify.ts`
2. Delete `src/__tests__/verify.test.ts` (tests for the dead code)
3. Check if any types or functions in `src/types.ts` or `src/state/blackboard.ts` are only used by `verify.ts` — if so, remove those too
4. Run `bun test` to confirm nothing breaks
5. Check if the skill registry in `src/skills/registry.ts` references `verify` — if so, remove that reference

## Verification

- `bun test` passes
- No imports of `verify.ts` remain anywhere in `src/`
- No orphaned types/functions left behind

## What NOT to change

- Do not modify the behaviour tree
- Do not remove `src/verify/` directory (that's the invariants verification pipeline — completely different from skills/verify.ts)
- Do not touch wiki pages

## Decision Rationale

Chosen over test coverage (candidates #1, #5) because the setup signal says to prefer implementation and improvement over more tests. Chosen over process temperature (#3) because that's a larger architectural change requiring wiki updates and tree modifications — better suited for a dedicated design cycle. Chosen over doc-sync (#4) because it's lower impact. Removing dead code is a clean, safe improvement that reduces confusion for future elves.
