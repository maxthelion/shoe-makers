# Work Item: Add insight-frequency config mention to creative-exploration.md

skill-type: doc-sync

## Context

The wiki page `wiki/pages/creative-exploration.md` describes the innovate action as always fetching a Wikipedia article. But there's also a probabilistic lens for `explore` actions controlled by `insight-frequency` config (default 0.3, meaning 30% of explore cycles get a Wikipedia lens). This is documented in `wiki/pages/integration.md` (line 95) but not in the creative-exploration page.

## What to do

Add a brief note to `wiki/pages/creative-exploration.md` in the "How It Works" section explaining:
- During `explore`, a Wikipedia lens is included probabilistically based on `insight-frequency` config (default 30% of explore cycles)
- During `innovate`, a Wikipedia article is always fetched
- The `insight-frequency` config is set in `.shoe-makers/config.yaml`

## What NOT to change

- Do not restructure the page
- Do not modify other wiki pages
- Do not modify code

## Decision Rationale

The creative-exploration wiki page is the spec for how creative prompting works. It should mention the explore-mode probabilistic lens since it's a designed behavior, not an implementation detail. The config is already documented in integration.md but should be cross-referenced here.
