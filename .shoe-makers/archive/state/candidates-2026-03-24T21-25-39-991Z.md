# Candidates

## 1. Add test for loadSkills filtering by enabledSkills config
**Type**: test-coverage
**Impact**: medium
**Reasoning**: `loadSkills()` in `src/skills/registry.ts` (line 96-134) accepts an `enabledSkills` parameter to filter which skills are loaded. This filtering behavior affects which skills appear in explore/execute prompts. Edge cases to test: empty array (no skills), array with unknown skill names (should be ignored), null (all enabled). The existing `src/__tests__/registry.test.ts` tests skill loading but not the filtering. Files: `src/skills/registry.ts`, `src/__tests__/registry.test.ts`.

## 2. Add test for shift summary edge cases
**Type**: test-coverage
**Impact**: low
**Reasoning**: `src/log/shift-summary.ts` (307 lines) is the largest non-test source file. It generates shift summaries. While `src/__tests__/shift-summary.test.ts` exists, the summary generation may have edge cases around empty shifts, shifts with only housekeeping, or shifts with unknown action types. Files: `src/log/shift-summary.ts`, `src/__tests__/shift-summary.test.ts`.

## 3. Verify wiki/pages/skills.md matches current skill registry
**Type**: doc-sync
**Impact**: low
**Reasoning**: The `wiki/pages/skills.md` page describes the skill system. With 9 skills now implemented, the page should list all of them accurately. If the page still references "planned" skills, it should be updated. Files: `wiki/pages/skills.md`, `.shoe-makers/skills/`.
