# Update tick-types.md — sync tree diagram and add innovate/evaluate-insight phases

skill-type: doc-sync

## What needs to change

`wiki/pages/tick-types.md` has a stale tree diagram (lines 13-23) missing three nodes that exist in the actual tree (`src/tree/default-tree.ts`):
- `[dead-code work-item?] → Remove dead code` (between inbox and work-item)
- `[insights exist?] → Evaluate insight` (after candidates)
- `[innovation tier?] → Innovate` (after insights)
- The final node should be `[always] → Explore` not `[neither?] → Explore`

The "Three-Phase Orchestration" section (lines 27-33) also doesn't mention innovate or evaluate-insight phases.

## Exact changes

### 1. Replace the tree diagram (lines 13-23) with:

```
Selector
├── [tests failing?]         → Fix them
├── [unresolved critiques?]  → Fix the flagged issues
├── [unreviewed commits?]    → Review adversarially
├── [uncommitted changes?]   → Review before committing
├── [inbox messages?]        → Read and act
├── [dead-code work-item?]   → Remove dead code
├── [work-item.md exists?]   → Execute the work item
├── [candidates.md exists?]  → Prioritise: pick one, write work-item.md
├── [insights exist?]        → Evaluate insight (generous disposition)
├── [innovation tier?]       → Innovate: write insight from creative brief
└── [always]                 → Explore: write candidates.md
```

### 2. Update the "Three-Phase Orchestration" section to mention the innovation phases:

After the existing 3 phases, add:

At innovation tier (all invariants met, health good), two additional actions:
4. **Innovate** — receives a creative brief (wiki overview + random Wikipedia article), writes an insight file
5. **Evaluate-insight** — when insight files exist, evaluates with generous disposition: promote, rework, or dismiss

## What NOT to change

- Do not modify src/, tests, or other wiki pages
- Do not modify .shoe-makers/invariants.md
- Keep the existing content structure — only update the stale parts

## Decision Rationale

The tick-types.md tree diagram is stale — it's missing 3 nodes that were added to the behaviour tree. This is a straightforward doc-sync that keeps the wiki accurate. Chosen over test candidates because the setup prompt says to prefer improvements over tests when coverage is already solid.
