# Candidates

## 1. Verify architecture.md data contracts match current Assessment type
**Type**: doc-sync
**Impact**: medium
**Reasoning**: `wiki/pages/architecture.md` describes the system architecture including data contracts. The Assessment type in `src/types.ts` now includes fields like `processPatterns`, `uncertainties`, `worstFiles`, `healthScore`, and `typecheckPass` that may not be reflected in the architecture page. Keeping the data contracts section accurate helps future elves understand what state is available. Files: `wiki/pages/architecture.md`, `src/types.ts`.

## 2. Add tests for shift-log-parser boundary conditions
**Type**: test
**Impact**: low
**Reasoning**: `computeProcessPatterns()` in `src/log/shift-log-parser.ts` has specific logic for review loop detection (3+ consecutive critique/fix-critique). Boundary cases (exactly 2 consecutive, exactly 3, mixed with other actions) should be verified. The existing tests in `src/__tests__/shift-log-parser.test.ts` may not cover all combinations. Files: `src/log/shift-log-parser.ts`, `src/__tests__/shift-log-parser.test.ts`.

## 3. Add README config example for max-ticks-per-shift and max-innovation-cycles
**Type**: doc-sync
**Impact**: low
**Reasoning**: README.md config example (lines 105-111) only shows 5 keys. Adding `max-ticks-per-shift: 10` and `max-innovation-cycles: 3` would make the README consistent with the integration guide's full table. Files: `README.md`.

## 4. Add test for loadSkills filtering by enabledSkills config
**Type**: test
**Impact**: low
**Reasoning**: `loadSkills()` in `src/skills/registry.ts` accepts an `enabledSkills` parameter to filter which skills are loaded. This filtering behavior should be tested — what happens with an empty list, a list with unknown skill names, null (all enabled). Files: `src/skills/registry.ts`, `src/__tests__/registry.test.ts`.
