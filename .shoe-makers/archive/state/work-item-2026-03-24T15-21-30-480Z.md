# Add claim-evidence entries and tests for Section 2.6.1 innovate observability

skill-type: implement

## Background

All 5 specified-only invariants are from section 2.6.1 "Innovate observability" in `.shoe-makers/invariants.md` (lines 125-129). The code partially exists — setup.ts already logs the Wikipedia article title, and the prompt enforces the lens — but there are no entries in `.shoe-makers/claim-evidence.yaml` and no tests verifying these behaviours.

## Spec (from invariants.md section 2.6.1)

> ### 2.6.1 Innovate observability
> - The setup script logs which Wikipedia article was fetched (title) to the shift log, or logs that the fetch failed
> - The innovate prompt output (the insight file) must include the Wikipedia article title in the Lens section — if an insight doesn't reference the article, the elf ignored the brief
> - The shift log entry for an innovate tick should include: the Wikipedia article title, whether an insight was written, and the insight filename
> - This observability allows the human to verify in the morning review that the creative pipeline is actually using random outside concepts, not falling back to general knowledge

## Existing code

### setup.ts (lines 86-99) — Wikipedia fetch + logging
```typescript
let article: { title: string; summary: string } | undefined;
if (skill === "innovate") {
  article = (await fetchRandomArticle()) ?? undefined;
  if (article) {
    await appendToShiftLog(repoRoot, `Wikipedia article fetched: "${article.title}"\n`);
  } else {
    await appendToShiftLog(repoRoot, `Wikipedia article fetch failed\n`);
  }
}
```

### three-phase.ts (lines 201-214) — innovate prompt
The prompt already includes:
- "Your insight **MUST** use the Wikipedia article provided above as the lens"
- Lens section format: `Start with the article title (e.g. "**${article.title}** — ...")`

### prompts.test.ts (lines 448-482) — existing innovate tests
Tests already verify the prompt contains article title, mandates insight file, says "No connection found" is not acceptable, mentions divergent/creative mode, and mentions off-limits.

## The 5 claim IDs that need evidence entries

1. `spec.innovate-observability.the-setup-script-logs-which-wikipedia-article-was-fetched-ti`
2. `spec.innovate-observability.the-innovate-prompt-output-the-insight-file-must-include-the`
3. `spec.innovate-observability.the-shift-log-entry-for-an-innovate-tick-should-include-the-`
4. `spec.innovate-observability.this-observability-allows-the-human-to-verify-in-the-morning`
5. `spec.creative-exploration-dedicated-innovate-and-evaluate-insight-actions.the-insight-file-must-reference-the-wikipedia-article-that-w`

**IMPORTANT**: Claim ID 5 is under the parent section `creative-exploration-dedicated-innovate-and-evaluate-insight-actions`, not under `innovate-observability`. This is because it was added as a bullet in section 2.6, not 2.6.1. Verify the exact IDs by running:
```
bun -e 'import { extractInvariantClaims } from "./src/verify/extract-claims"; const claims = await extractInvariantClaims("."); for (const c of claims.filter(c => c.id.includes("wikipedia") || c.id.includes("observability-allows"))) console.log(c.id);'
```

## What to build

### 1. Add claim-evidence entries to `.shoe-makers/claim-evidence.yaml`

Add entries at the end of the file (before any trailing newlines), following the existing pattern. Each entry needs `source:` (patterns that match source code) and `test:` (patterns that match test code). Evidence is case-insensitive substring matching against all file contents (comments stripped).

**Entry patterns to add** (evidence patterns must match actual code strings, not comments):

```yaml
# === innovate observability (invariants.md 2.6.1) ===

# The insight file must reference the Wikipedia article (section 2.6 bullet)
spec.creative-exploration-dedicated-innovate-and-evaluate-insight-actions.the-insight-file-must-reference-the-wikipedia-article-that-w:
  source:
    - [MUST use the Wikipedia article]
  test:
    - [Wikipedia article]

# Setup logs which Wikipedia article was fetched
spec.innovate-observability.the-setup-script-logs-which-wikipedia-article-was-fetched-ti:
  source:
    - [Wikipedia article fetched]
  test:
    - [Wikipedia article fetched]

# Insight file must include article title in Lens section
spec.innovate-observability.the-innovate-prompt-output-the-insight-file-must-include-the:
  source:
    - [Start with the article title]
  test:
    - [article title]

# Shift log entry for innovate tick should include article, insight written, filename
spec.innovate-observability.the-shift-log-entry-for-an-innovate-tick-should-include-the-:
  source:
    - [Wikipedia article fetched]
  test:
    - [Wikipedia article fetched]

# Observability for morning review
spec.innovate-observability.this-observability-allows-the-human-to-verify-in-the-morning:
  source:
    - [Wikipedia article fetched]
    - [Wikipedia article fetch failed]
  test:
    - [Wikipedia article fetched]
    - [Wikipedia article fetch failed]
```

### 2. Add tests to `src/__tests__/prompts.test.ts`

Add to the existing `describe("innovate prompt")` block (after line 481):

```typescript
test("requires Wikipedia article as the lens, not general knowledge", () => {
  const prompt = generatePrompt("innovate", makeState(), undefined, article, undefined, wikiSummary);
  expect(prompt).toContain("MUST use the Wikipedia article");
  expect(prompt).toContain("Do not use general knowledge");
});

test("Lens section format starts with article title", () => {
  const prompt = generatePrompt("innovate", makeState(), undefined, article, undefined, wikiSummary);
  expect(prompt).toContain("Start with the article title");
  expect(prompt).toContain("Mycelial Networks");
});
```

### 3. Add tests to `src/__tests__/setup.test.ts` (or the appropriate test file)

Test that setup.ts logs the Wikipedia article to the shift log when innovate action is selected. Look at existing setup tests for the pattern — they likely mock `fetchRandomArticle` and check shift log output. Add:

```typescript
test("logs Wikipedia article fetched to shift log for innovate", async () => {
  // ... setup mocks for innovate action with article
  // ... run setup
  // ... verify shift log contains 'Wikipedia article fetched: "ArticleTitle"'
});

test("logs Wikipedia article fetch failed to shift log for innovate", async () => {
  // ... setup mocks where fetchRandomArticle returns null
  // ... run setup
  // ... verify shift log contains 'Wikipedia article fetch failed'
});
```

Look at existing tests in setup.test.ts for mock patterns. The tests need to match the evidence patterns above.

## What NOT to change

- `.shoe-makers/invariants.md` — off-limits, only humans modify
- The actual behaviour in `setup.ts` or `three-phase.ts` — the code is already correct, we're just adding evidence + tests
- Other claim-evidence entries — don't modify existing ones
- Test patterns for other claims — leave them alone

## Verification

After implementing, run:
1. `bun test` — all tests must pass
2. `bunx tsc --noEmit` — typecheck must pass
3. `bun run setup` — should show 0 specified-only invariants (down from 5)

## Decision Rationale

Candidate 1 was chosen because it directly addresses all 5 specified-only invariants with minimal code changes (evidence entries + tests only, no new behaviour needed). This has the highest impact-to-effort ratio — it closes the gap between spec and verified implementation. Candidates 2-5 are either subsets of this work or lower priority improvements that can follow once the invariant pipeline is green.
