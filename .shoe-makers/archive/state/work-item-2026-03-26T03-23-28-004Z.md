skill-type: test-coverage

# Close the last untested invariant: housekeeping deterministic operations

## Wiki Spec

From `wiki/pages/verification.md` and invariants: "Housekeeping tasks (archiving, shift log updates) should be fully deterministic — no LLM judgement needed, setup handles them directly."

## Current Code

- `src/setup/housekeeping.ts` — `autoCommitHousekeeping()` (lines 33-80), `isAllHousekeeping()` (lines 16-24)
- `src/skills/assess.ts` — `archiveResolvedFindings()` (lines 91-121)
- `src/__tests__/auto-commit-housekeeping.test.ts` — tests `isAllHousekeeping` (11 tests) but doesn't mention `autoCommitHousekeeping`
- `src/__tests__/archive.test.ts` — tests `archiveResolvedFindings` (8 tests, string found in tests)

## What to Build

The claim-evidence at `.shoe-makers/claim-evidence/25-structured-skills.yaml` lines 50-57 has test evidence patterns `[autoCommitHousekeeping]` and `[archiveResolvedFindings]`. The `archiveResolvedFindings` pattern matches `archive.test.ts`. But `autoCommitHousekeeping` doesn't appear in any test file.

Fix: Add a test for `autoCommitHousekeeping` to `src/__tests__/auto-commit-housekeeping.test.ts`. The function requires a git repo, so tests need `git init`, `git add`, `git commit` setup in a temp directory. Test that:

1. `autoCommitHousekeeping` commits when all changes are housekeeping
2. `autoCommitHousekeeping` skips when non-housekeeping changes exist
3. `autoCommitHousekeeping` advances the `last-reviewed-commit` marker when the auto-commit is the only unreviewed commit
4. `autoCommitHousekeeping` does NOT advance the marker when there are other unreviewed commits

Import `autoCommitHousekeeping` directly from `../setup/housekeeping` so the string appears in the test file.

## Patterns to Follow

Follow `archive.test.ts` pattern: temp dir with `mkdtemp`, cleanup with `rm`, test actual file system behavior.

## Tests to Write

See "What to Build" above.

## What NOT to Change

- Don't modify the `autoCommitHousekeeping` function itself
- Don't modify the claim-evidence YAML
- Don't modify invariants.md

## Decision Rationale

This is the only untested invariant (1 untested, per assessment). Closing it brings the untested count to 0 and completes test coverage for all invariants.
