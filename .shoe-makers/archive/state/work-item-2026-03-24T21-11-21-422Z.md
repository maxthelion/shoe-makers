# Add missing config keys to README config example

skill-type: doc-sync

## What needs to change

`README.md` lines 105-111 show a config example with 5 keys. The actual config system (`src/config/load-config.ts` lines 7-16) supports 8 keys. Add the 3 missing keys to the README example:

Current (README lines 105-111):
```yaml
branch-prefix: shoemakers
tick-interval: 5
wiki-dir: wiki
assessment-stale-after: 30
insight-frequency: 0.3
```

Should be:
```yaml
branch-prefix: shoemakers
tick-interval: 5
wiki-dir: wiki
assessment-stale-after: 30
max-ticks-per-shift: 10
enabled-skills: fix-tests, implement, test-coverage, doc-sync, health
insight-frequency: 0.3
max-innovation-cycles: 3
```

The values shown should match the defaults from `src/config/load-config.ts` lines 18-27 (DEFAULTS object). For `enabled-skills`, show an example list (since the default is `null` meaning all enabled, show a representative subset with a comment or note that omitting it enables all skills).

## What NOT to change

- Do not modify src/, tests, or wiki pages
- Do not modify .shoe-makers/invariants.md
- Do not change any other section of README.md
- Keep the existing prose around the config example unchanged

## Decision Rationale

Picked over test candidates because the setup prompt says to prefer improvement over tests. This doc-sync has appeared in 3+ explore cycles without being actioned. Invariant 3.5 says "README reflects current capabilities" and invariant 5.2 lists all these config keys as configurable. Simple, low-risk, addresses a real gap.
