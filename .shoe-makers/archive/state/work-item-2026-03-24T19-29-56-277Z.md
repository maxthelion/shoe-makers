# Work Item: Add tests for buildEvaluateInsightPrompt and buildInnovatePrompt

skill-type: test

## Context

Two prompt builder functions in `src/prompts/three-phase.ts` have insufficient test coverage:
- `buildEvaluateInsightPrompt()` — completely untested, returns a static prompt string
- `buildInnovatePrompt(wikiSummary, article?)` — partially tested, missing tests for undefined article path and wiki summary formatting

## What to do

Add tests to `src/__tests__/prompts.test.ts`:

1. **`buildEvaluateInsightPrompt()`**:
   - Test it returns a string containing key instructions ("generous disposition", "constructive/convergent mode")
   - Test it includes the three decision options (Promote, Rework, Dismiss)
   - Test it includes off-limits notice

2. **`buildInnovatePrompt(wikiSummary, article)`**:
   - Test with article: includes article title and summary in output
   - Test without article (undefined): falls back to "choose your own creative lens" path
   - Test wiki summary is included in output
   - Test off-limits notice is present

## Patterns to follow

Look at existing tests in `src/__tests__/prompts.test.ts` for the `generatePrompt()` function. Import `buildEvaluateInsightPrompt` and `buildInnovatePrompt` directly from `../prompts/three-phase`.

## What NOT to change

- Do not modify source code, only add tests
- Do not modify other test files

## Decision Rationale

Combined candidates #1 and #2 since they're in the same source file. These are the last major untested prompt paths in the creative pipeline.
