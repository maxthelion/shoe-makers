# Verify and fix README accuracy

skill-type: doc-sync

## Context

Invariant 3.5 in `.shoe-makers/invariants.md` claims the explore step checks README accuracy. The README (149 lines) appears comprehensive, but a systematic verification has not been done.

## What to check

1. **Tree diagram** (lines 11-24): Compare against `src/tree/default-tree.ts` — does every node match?
2. **Config keys** (lines 106-114): Compare each key against `src/config/load-config.ts` — are any documented keys missing from the code, or any code keys missing from the docs?
3. **Project structure** (lines 124-140): Run `ls` on the actual directories and compare
4. **Scripts** (lines 89-93): Check `package.json` scripts section — are all listed scripts real?
5. **Getting started instructions** (lines 56-93): Do the commands actually work?

## What to fix

If any drift is found, edit `README.md` to match the current codebase. The code is the truth for this check (README should describe what exists, not what should exist).

## What NOT to change

- Do not modify source files
- Do not modify tests
- Do not modify wiki pages
- Do not modify `.shoe-makers/invariants.md`
- Do not add new features or sections — only fix drift in existing content

## Tests to write

None — this is a documentation fix. Run `bun test` to confirm no tests were broken by any accidental changes.

## Decision Rationale

Candidate #2 (README audit) chosen over #1 (fs.ts tests — low impact, trivial utility) and #3 (dead-code test gap — low impact, single test case). The README audit fulfills an invariant claim (3.5) that hasn't been verified, and documentation drift can mislead new users and contributors.
