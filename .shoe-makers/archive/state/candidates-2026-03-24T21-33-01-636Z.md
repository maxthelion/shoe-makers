# Candidates

## 1. Add `Review Uncommitted Work` action to TITLE_TO_ACTION docs
**Type**: doc-sync
**Impact**: low
**Reasoning**: The `TITLE_TO_ACTION` mapping in `src/log/shift-log-parser.ts` maps "Review Uncommitted Work" to the "review" action. The `REACTIVE_ACTIONS` set in `src/log/action-constants.ts` includes "review". However, invariant 2.2 lists reactive conditions without mentioning "uncommitted changes" — it only lists tests, critiques, unreviewed commits, and inbox. The invariant is stale (requires human update). Worth noting as a finding. Files: `.shoe-makers/invariants.md` (needs human update), `wiki/pages/behaviour-tree.md`.

## 2. Verify shift-summary.ts handles empty action lists gracefully
**Type**: test-coverage
**Impact**: low
**Reasoning**: `src/log/shift-summary.ts` (307 lines) is the largest non-test source file. It generates shift summaries from action lists. While `src/__tests__/shift-summary.test.ts` (342 lines) exists, edge cases around zero-length shifts or shifts with only unknown actions should be verified. Files: `src/log/shift-summary.ts`, `src/__tests__/shift-summary.test.ts`.

## 3. Check for stale wiki page links (wikilinks)
**Type**: doc-sync
**Impact**: low
**Reasoning**: Wiki pages use `[[page-name|display text]]` wikilinks. If any linked pages have been renamed or removed, the links would be broken. A scan of all wiki pages for wikilinks and verification against the actual page filenames would catch stale links. Files: `wiki/pages/*.md`.
