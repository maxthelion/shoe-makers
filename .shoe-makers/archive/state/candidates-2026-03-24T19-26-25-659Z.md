# Candidates

## 1. Add tests for prompt helper functions (determineTier, isInnovationTier, formatTopGaps)
**Type**: test
**Impact**: medium
**Reasoning**: `src/prompts/helpers.ts` contains pure functions controlling tier routing and gap presentation — `determineTier()`, `isInnovationTier()`, `formatTopGaps()`, `formatCodebaseSnapshot()`. These have no direct test coverage. Edge cases like empty gaps, tier boundary conditions (specOnlyCount=0 vs >0), and missing assessment data are unverified. Files: `src/prompts/helpers.ts`, needs new `src/__tests__/prompt-helpers.test.ts`.

## 2. Add tests for buildEvaluateInsightPrompt (completely untested)
**Type**: test
**Impact**: medium
**Reasoning**: `buildEvaluateInsightPrompt()` in `src/prompts/three-phase.ts` generates the prompt for the generous insight evaluator — a critical creative pipeline function with zero test coverage. The function includes complex formatting of insight count and disposition instructions. Files: `src/prompts/three-phase.ts`, `src/__tests__/prompts.test.ts`.

## 3. Process temperature signal influencing tree routing
**Type**: implement
**Impact**: medium
**Reasoning**: The system detects high reactive ratio (>60%) but the signal only appears in explore prompts. Adding a tree condition or advisory note when reactive ratio is persistently high could help break churn cycles. This requires careful design to avoid disrupting the priority ordering. Spec: `wiki/pages/behaviour-tree.md`. Files: `src/tree/default-tree.ts`, `src/log/shift-summary.ts`, `src/state/world.ts`.

## 4. Add test for fetchArticleForAction Wikipedia integration
**Type**: test
**Impact**: low
**Reasoning**: `fetchArticleForAction()` in `src/creative/wikipedia.ts` is the main entry point for the creative pipeline's Wikipedia integration. While `shouldIncludeLens()` and `fetchRandomArticle()` have some test coverage, the combined `fetchArticleForAction()` function (which decides based on skill type and config) could benefit from tests verifying the skill-type routing logic. Files: `src/creative/wikipedia.ts`, `src/__tests__/wikipedia.test.ts`.

## 5. Review and clean up archived findings for patterns
**Type**: doc-sync
**Impact**: low
**Reasoning**: `.shoe-makers/findings/archive/` has accumulated 40+ resolved critiques from this shift. Reviewing them for recurring patterns could inform known-issues.md updates or identify systemic improvements. Not urgent but useful for shift summary quality.
