# Candidates

## 1. Implement structured explore skill — enforce candidate output format with pre-filled context
**Type**: implement
**Impact**: high
**Reasoning**: Per `wiki/pages/structured-skills.md` lines 74-77, the `write-candidates` skill should have setup provide invariant counts, health scores, findings, and tier classification, with the template enforcing minimum 3 candidates and required fields (type, impact, reasoning). The explore prompt (`src/prompts/explore.ts`) gives guidance but doesn't use pre-filled output templates with `[YOUR JUDGEMENT HERE]` placeholders. Following the pattern just implemented for structured critique in `src/setup/critique-context.ts`, create `src/setup/explore-context.ts` that gathers codebase metrics and builds a structured output template. This closes specified-only invariant gaps for structured skills.

## 2. Update verification wiki to document review-loop-with-candidates node
**Type**: doc-sync
**Impact**: medium
**Reasoning**: Advisory finding from critique-2026-03-26-002: `wiki/pages/verification.md` tree order diagram (lines 124-138) doesn't include the `review-loop-with-candidates` node added in `src/tree/default-tree.ts:109`. The code has TWO review-loop nodes (one routes to prioritise when candidates exist, one to explore), but the wiki only documents the single explore route. The wiki-as-spec principle requires keeping the spec current. Simple doc-sync: add the new node to the tree diagram and explain its purpose.

## 3. Reduce prompts.test.ts complexity — extract helper tests to prompt-helpers.test.ts
**Type**: octoclean-fix
**Impact**: medium
**Reasoning**: `src/__tests__/prompts.test.ts` is the worst-scoring file at 87/100 health with 739 lines. It tests both `generatePrompt` integration and helper functions (`determineTier`, `isInnovationTier`, `findSkillForAction`, formatters). The helper-function tests could move to `src/__tests__/prompt-helpers.test.ts` which already exists at 93/100 health. Moving ~200 lines of helper tests would reduce prompts.test.ts to ~540 lines and improve both files' health scores. This was identified in the previous explore cycle.

## 4. Remove hardcoded FALLBACK_CONCEPTS from wikipedia.ts — replace with corpus-only fallback
**Type**: dead-code
**Impact**: low
**Reasoning**: `src/creative/wikipedia.ts` lines 5-77 contain a 50+ item `FALLBACK_CONCEPTS` hardcoded array (77 lines of data). Now that the local corpus reader is implemented, the hardcoded array is a tertiary fallback (Wikipedia → corpus → hardcoded). The corpus has 20 articles which provides sufficient fallback diversity. Removing the hardcoded array would reduce wikipedia.ts by ~75 lines and eliminate duplicated concept data. However, this is low impact since the array doesn't hurt anything being there and provides defense-in-depth.
