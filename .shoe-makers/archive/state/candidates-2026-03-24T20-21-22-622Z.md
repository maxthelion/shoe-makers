# Candidates

## 1. Add tests for config loader (src/config/load-config.ts)
**Type**: test
**Impact**: medium
**Reasoning**: `src/config/load-config.ts` is untested — it handles parsing `config.yaml` with defaults and validation, and is used by every setup invocation. The config test file (`src/__tests__/config.test.ts`) exists but may only test specific config validation, not the full `loadConfig()` flow. A config parsing bug could silently break tick interval, enabled skills, or innovation limits. Files: `src/config/load-config.ts`, `src/__tests__/config.test.ts`.

## 2. Sync skills.md wiki page — all planned skills are now implemented
**Type**: doc-sync
**Impact**: medium
**Reasoning**: `wiki/pages/skills.md` section "### 3.2 Skills" states "Planned: octoclean-fix, bug-fix, dependency-update, dead-code removal" but all are now implemented in `.shoe-makers/skills/`. The wiki page should be updated to reflect that all 9 skills are current. This is the wiki-side companion to the stale-invariants finding (invariants.md requires human update, but skills.md can be updated by elves). Files: `wiki/pages/skills.md`, `.shoe-makers/skills/`.

## 3. Add test for state-archive edge cases (archiveConsumedStateFiles)
**Type**: test
**Impact**: low
**Reasoning**: `src/archive/state-archive.ts` handles archiving state files (candidates.md, work-item.md) for traceability. The existing test (`src/__tests__/state-archive.test.ts`) covers basic archival but may not cover edge cases like missing directories, concurrent archives with the same timestamp, or files that don't exist. Files: `src/archive/state-archive.ts`, `src/__tests__/state-archive.test.ts`.

## 4. Sync open-questions.md — check which questions are now resolved
**Type**: doc-sync
**Impact**: low
**Reasoning**: `wiki/pages/open-questions.md` may contain questions that have been answered by subsequent implementation. Since the system has been operational for multiple shifts and many features have been built, some open questions may now have answers in the code. Reviewing and closing resolved questions keeps the spec clean. Files: `wiki/pages/open-questions.md`.
